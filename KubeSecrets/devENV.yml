apiVersion: v1
kind: Secret
metadata:
  name: ordering-backend-secrets
  namespace: ordering
type: Opaque
stringData:
  # Postgres connection URL - Set via GitHub Secrets
  # Key must match what's referenced in values.yaml secretKeyRef
  ORDERING_POSTGRES_URL: "${ORDERING_POSTGRES_URL}"
