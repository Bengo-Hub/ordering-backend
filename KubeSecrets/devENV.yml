apiVersion: v1
kind: Secret
metadata:
  name: ordering-backend-secrets
  namespace: ordering
type: Opaque
stringData:
  # Postgres connection URL - Set via GitHub Secrets
  postgresUrl: "${ORDERING_POSTGRES_URL}"
