// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/cafe-backend/internal/ent/backupcode"
	"github.com/bengobox/cafe-backend/internal/ent/user"
	"github.com/google/uuid"
)

// BackupCodeCreate is the builder for creating a BackupCode entity.
type BackupCodeCreate struct {
	config
	mutation *BackupCodeMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetCodeHash sets the "code_hash" field.
func (bcc *BackupCodeCreate) SetCodeHash(s string) *BackupCodeCreate {
	bcc.mutation.SetCodeHash(s)
	return bcc
}

// SetUsedAt sets the "used_at" field.
func (bcc *BackupCodeCreate) SetUsedAt(t time.Time) *BackupCodeCreate {
	bcc.mutation.SetUsedAt(t)
	return bcc
}

// SetNillableUsedAt sets the "used_at" field if the given value is not nil.
func (bcc *BackupCodeCreate) SetNillableUsedAt(t *time.Time) *BackupCodeCreate {
	if t != nil {
		bcc.SetUsedAt(*t)
	}
	return bcc
}

// SetCreatedAt sets the "created_at" field.
func (bcc *BackupCodeCreate) SetCreatedAt(t time.Time) *BackupCodeCreate {
	bcc.mutation.SetCreatedAt(t)
	return bcc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (bcc *BackupCodeCreate) SetNillableCreatedAt(t *time.Time) *BackupCodeCreate {
	if t != nil {
		bcc.SetCreatedAt(*t)
	}
	return bcc
}

// SetID sets the "id" field.
func (bcc *BackupCodeCreate) SetID(u uuid.UUID) *BackupCodeCreate {
	bcc.mutation.SetID(u)
	return bcc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (bcc *BackupCodeCreate) SetNillableID(u *uuid.UUID) *BackupCodeCreate {
	if u != nil {
		bcc.SetID(*u)
	}
	return bcc
}

// SetUserID sets the "user" edge to the User entity by ID.
func (bcc *BackupCodeCreate) SetUserID(id uuid.UUID) *BackupCodeCreate {
	bcc.mutation.SetUserID(id)
	return bcc
}

// SetUser sets the "user" edge to the User entity.
func (bcc *BackupCodeCreate) SetUser(u *User) *BackupCodeCreate {
	return bcc.SetUserID(u.ID)
}

// Mutation returns the BackupCodeMutation object of the builder.
func (bcc *BackupCodeCreate) Mutation() *BackupCodeMutation {
	return bcc.mutation
}

// Save creates the BackupCode in the database.
func (bcc *BackupCodeCreate) Save(ctx context.Context) (*BackupCode, error) {
	bcc.defaults()
	return withHooks(ctx, bcc.sqlSave, bcc.mutation, bcc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (bcc *BackupCodeCreate) SaveX(ctx context.Context) *BackupCode {
	v, err := bcc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (bcc *BackupCodeCreate) Exec(ctx context.Context) error {
	_, err := bcc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bcc *BackupCodeCreate) ExecX(ctx context.Context) {
	if err := bcc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (bcc *BackupCodeCreate) defaults() {
	if _, ok := bcc.mutation.CreatedAt(); !ok {
		v := backupcode.DefaultCreatedAt()
		bcc.mutation.SetCreatedAt(v)
	}
	if _, ok := bcc.mutation.ID(); !ok {
		v := backupcode.DefaultID()
		bcc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (bcc *BackupCodeCreate) check() error {
	if _, ok := bcc.mutation.CodeHash(); !ok {
		return &ValidationError{Name: "code_hash", err: errors.New(`ent: missing required field "BackupCode.code_hash"`)}
	}
	if v, ok := bcc.mutation.CodeHash(); ok {
		if err := backupcode.CodeHashValidator(v); err != nil {
			return &ValidationError{Name: "code_hash", err: fmt.Errorf(`ent: validator failed for field "BackupCode.code_hash": %w`, err)}
		}
	}
	if _, ok := bcc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "BackupCode.created_at"`)}
	}
	if _, ok := bcc.mutation.UserID(); !ok {
		return &ValidationError{Name: "user", err: errors.New(`ent: missing required edge "BackupCode.user"`)}
	}
	return nil
}

func (bcc *BackupCodeCreate) sqlSave(ctx context.Context) (*BackupCode, error) {
	if err := bcc.check(); err != nil {
		return nil, err
	}
	_node, _spec := bcc.createSpec()
	if err := sqlgraph.CreateNode(ctx, bcc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	bcc.mutation.id = &_node.ID
	bcc.mutation.done = true
	return _node, nil
}

func (bcc *BackupCodeCreate) createSpec() (*BackupCode, *sqlgraph.CreateSpec) {
	var (
		_node = &BackupCode{config: bcc.config}
		_spec = sqlgraph.NewCreateSpec(backupcode.Table, sqlgraph.NewFieldSpec(backupcode.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = bcc.conflict
	if id, ok := bcc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := bcc.mutation.CodeHash(); ok {
		_spec.SetField(backupcode.FieldCodeHash, field.TypeString, value)
		_node.CodeHash = value
	}
	if value, ok := bcc.mutation.UsedAt(); ok {
		_spec.SetField(backupcode.FieldUsedAt, field.TypeTime, value)
		_node.UsedAt = value
	}
	if value, ok := bcc.mutation.CreatedAt(); ok {
		_spec.SetField(backupcode.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if nodes := bcc.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   backupcode.UserTable,
			Columns: []string{backupcode.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.backup_code_user = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.BackupCode.Create().
//		SetCodeHash(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.BackupCodeUpsert) {
//			SetCodeHash(v+v).
//		}).
//		Exec(ctx)
func (bcc *BackupCodeCreate) OnConflict(opts ...sql.ConflictOption) *BackupCodeUpsertOne {
	bcc.conflict = opts
	return &BackupCodeUpsertOne{
		create: bcc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (bcc *BackupCodeCreate) OnConflictColumns(columns ...string) *BackupCodeUpsertOne {
	bcc.conflict = append(bcc.conflict, sql.ConflictColumns(columns...))
	return &BackupCodeUpsertOne{
		create: bcc,
	}
}

type (
	// BackupCodeUpsertOne is the builder for "upsert"-ing
	//  one BackupCode node.
	BackupCodeUpsertOne struct {
		create *BackupCodeCreate
	}

	// BackupCodeUpsert is the "OnConflict" setter.
	BackupCodeUpsert struct {
		*sql.UpdateSet
	}
)

// SetCodeHash sets the "code_hash" field.
func (u *BackupCodeUpsert) SetCodeHash(v string) *BackupCodeUpsert {
	u.Set(backupcode.FieldCodeHash, v)
	return u
}

// UpdateCodeHash sets the "code_hash" field to the value that was provided on create.
func (u *BackupCodeUpsert) UpdateCodeHash() *BackupCodeUpsert {
	u.SetExcluded(backupcode.FieldCodeHash)
	return u
}

// SetUsedAt sets the "used_at" field.
func (u *BackupCodeUpsert) SetUsedAt(v time.Time) *BackupCodeUpsert {
	u.Set(backupcode.FieldUsedAt, v)
	return u
}

// UpdateUsedAt sets the "used_at" field to the value that was provided on create.
func (u *BackupCodeUpsert) UpdateUsedAt() *BackupCodeUpsert {
	u.SetExcluded(backupcode.FieldUsedAt)
	return u
}

// ClearUsedAt clears the value of the "used_at" field.
func (u *BackupCodeUpsert) ClearUsedAt() *BackupCodeUpsert {
	u.SetNull(backupcode.FieldUsedAt)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(backupcode.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *BackupCodeUpsertOne) UpdateNewValues() *BackupCodeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(backupcode.FieldID)
		}
		if _, exists := u.create.mutation.CreatedAt(); exists {
			s.SetIgnore(backupcode.FieldCreatedAt)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *BackupCodeUpsertOne) Ignore() *BackupCodeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *BackupCodeUpsertOne) DoNothing() *BackupCodeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the BackupCodeCreate.OnConflict
// documentation for more info.
func (u *BackupCodeUpsertOne) Update(set func(*BackupCodeUpsert)) *BackupCodeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&BackupCodeUpsert{UpdateSet: update})
	}))
	return u
}

// SetCodeHash sets the "code_hash" field.
func (u *BackupCodeUpsertOne) SetCodeHash(v string) *BackupCodeUpsertOne {
	return u.Update(func(s *BackupCodeUpsert) {
		s.SetCodeHash(v)
	})
}

// UpdateCodeHash sets the "code_hash" field to the value that was provided on create.
func (u *BackupCodeUpsertOne) UpdateCodeHash() *BackupCodeUpsertOne {
	return u.Update(func(s *BackupCodeUpsert) {
		s.UpdateCodeHash()
	})
}

// SetUsedAt sets the "used_at" field.
func (u *BackupCodeUpsertOne) SetUsedAt(v time.Time) *BackupCodeUpsertOne {
	return u.Update(func(s *BackupCodeUpsert) {
		s.SetUsedAt(v)
	})
}

// UpdateUsedAt sets the "used_at" field to the value that was provided on create.
func (u *BackupCodeUpsertOne) UpdateUsedAt() *BackupCodeUpsertOne {
	return u.Update(func(s *BackupCodeUpsert) {
		s.UpdateUsedAt()
	})
}

// ClearUsedAt clears the value of the "used_at" field.
func (u *BackupCodeUpsertOne) ClearUsedAt() *BackupCodeUpsertOne {
	return u.Update(func(s *BackupCodeUpsert) {
		s.ClearUsedAt()
	})
}

// Exec executes the query.
func (u *BackupCodeUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for BackupCodeCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *BackupCodeUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *BackupCodeUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: BackupCodeUpsertOne.ID is not supported by MySQL driver. Use BackupCodeUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *BackupCodeUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// BackupCodeCreateBulk is the builder for creating many BackupCode entities in bulk.
type BackupCodeCreateBulk struct {
	config
	err      error
	builders []*BackupCodeCreate
	conflict []sql.ConflictOption
}

// Save creates the BackupCode entities in the database.
func (bccb *BackupCodeCreateBulk) Save(ctx context.Context) ([]*BackupCode, error) {
	if bccb.err != nil {
		return nil, bccb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(bccb.builders))
	nodes := make([]*BackupCode, len(bccb.builders))
	mutators := make([]Mutator, len(bccb.builders))
	for i := range bccb.builders {
		func(i int, root context.Context) {
			builder := bccb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*BackupCodeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, bccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = bccb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, bccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, bccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (bccb *BackupCodeCreateBulk) SaveX(ctx context.Context) []*BackupCode {
	v, err := bccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (bccb *BackupCodeCreateBulk) Exec(ctx context.Context) error {
	_, err := bccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (bccb *BackupCodeCreateBulk) ExecX(ctx context.Context) {
	if err := bccb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.BackupCode.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.BackupCodeUpsert) {
//			SetCodeHash(v+v).
//		}).
//		Exec(ctx)
func (bccb *BackupCodeCreateBulk) OnConflict(opts ...sql.ConflictOption) *BackupCodeUpsertBulk {
	bccb.conflict = opts
	return &BackupCodeUpsertBulk{
		create: bccb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (bccb *BackupCodeCreateBulk) OnConflictColumns(columns ...string) *BackupCodeUpsertBulk {
	bccb.conflict = append(bccb.conflict, sql.ConflictColumns(columns...))
	return &BackupCodeUpsertBulk{
		create: bccb,
	}
}

// BackupCodeUpsertBulk is the builder for "upsert"-ing
// a bulk of BackupCode nodes.
type BackupCodeUpsertBulk struct {
	create *BackupCodeCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(backupcode.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *BackupCodeUpsertBulk) UpdateNewValues() *BackupCodeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(backupcode.FieldID)
			}
			if _, exists := b.mutation.CreatedAt(); exists {
				s.SetIgnore(backupcode.FieldCreatedAt)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.BackupCode.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *BackupCodeUpsertBulk) Ignore() *BackupCodeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *BackupCodeUpsertBulk) DoNothing() *BackupCodeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the BackupCodeCreateBulk.OnConflict
// documentation for more info.
func (u *BackupCodeUpsertBulk) Update(set func(*BackupCodeUpsert)) *BackupCodeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&BackupCodeUpsert{UpdateSet: update})
	}))
	return u
}

// SetCodeHash sets the "code_hash" field.
func (u *BackupCodeUpsertBulk) SetCodeHash(v string) *BackupCodeUpsertBulk {
	return u.Update(func(s *BackupCodeUpsert) {
		s.SetCodeHash(v)
	})
}

// UpdateCodeHash sets the "code_hash" field to the value that was provided on create.
func (u *BackupCodeUpsertBulk) UpdateCodeHash() *BackupCodeUpsertBulk {
	return u.Update(func(s *BackupCodeUpsert) {
		s.UpdateCodeHash()
	})
}

// SetUsedAt sets the "used_at" field.
func (u *BackupCodeUpsertBulk) SetUsedAt(v time.Time) *BackupCodeUpsertBulk {
	return u.Update(func(s *BackupCodeUpsert) {
		s.SetUsedAt(v)
	})
}

// UpdateUsedAt sets the "used_at" field to the value that was provided on create.
func (u *BackupCodeUpsertBulk) UpdateUsedAt() *BackupCodeUpsertBulk {
	return u.Update(func(s *BackupCodeUpsert) {
		s.UpdateUsedAt()
	})
}

// ClearUsedAt clears the value of the "used_at" field.
func (u *BackupCodeUpsertBulk) ClearUsedAt() *BackupCodeUpsertBulk {
	return u.Update(func(s *BackupCodeUpsert) {
		s.ClearUsedAt()
	})
}

// Exec executes the query.
func (u *BackupCodeUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the BackupCodeCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for BackupCodeCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *BackupCodeUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
