// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/cafe-backend/internal/ent/backupcode"
	"github.com/bengobox/cafe-backend/internal/ent/user"
	"github.com/google/uuid"
)

// BackupCode is the model entity for the BackupCode schema.
type BackupCode struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CodeHash holds the value of the "code_hash" field.
	CodeHash string `json:"code_hash,omitempty"`
	// UsedAt holds the value of the "used_at" field.
	UsedAt time.Time `json:"used_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BackupCodeQuery when eager-loading is set.
	Edges            BackupCodeEdges `json:"edges"`
	backup_code_user *uuid.UUID
	selectValues     sql.SelectValues
}

// BackupCodeEdges holds the relations/edges for other nodes in the graph.
type BackupCodeEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BackupCodeEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BackupCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case backupcode.FieldCodeHash:
			values[i] = new(sql.NullString)
		case backupcode.FieldUsedAt, backupcode.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case backupcode.FieldID:
			values[i] = new(uuid.UUID)
		case backupcode.ForeignKeys[0]: // backup_code_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BackupCode fields.
func (bc *BackupCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case backupcode.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				bc.ID = *value
			}
		case backupcode.FieldCodeHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code_hash", values[i])
			} else if value.Valid {
				bc.CodeHash = value.String
			}
		case backupcode.FieldUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field used_at", values[i])
			} else if value.Valid {
				bc.UsedAt = value.Time
			}
		case backupcode.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				bc.CreatedAt = value.Time
			}
		case backupcode.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field backup_code_user", values[i])
			} else if value.Valid {
				bc.backup_code_user = new(uuid.UUID)
				*bc.backup_code_user = *value.S.(*uuid.UUID)
			}
		default:
			bc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BackupCode.
// This includes values selected through modifiers, order, etc.
func (bc *BackupCode) Value(name string) (ent.Value, error) {
	return bc.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the BackupCode entity.
func (bc *BackupCode) QueryUser() *UserQuery {
	return NewBackupCodeClient(bc.config).QueryUser(bc)
}

// Update returns a builder for updating this BackupCode.
// Note that you need to call BackupCode.Unwrap() before calling this method if this BackupCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (bc *BackupCode) Update() *BackupCodeUpdateOne {
	return NewBackupCodeClient(bc.config).UpdateOne(bc)
}

// Unwrap unwraps the BackupCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (bc *BackupCode) Unwrap() *BackupCode {
	_tx, ok := bc.config.driver.(*txDriver)
	if !ok {
		panic("ent: BackupCode is not a transactional entity")
	}
	bc.config.driver = _tx.drv
	return bc
}

// String implements the fmt.Stringer.
func (bc *BackupCode) String() string {
	var builder strings.Builder
	builder.WriteString("BackupCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", bc.ID))
	builder.WriteString("code_hash=")
	builder.WriteString(bc.CodeHash)
	builder.WriteString(", ")
	builder.WriteString("used_at=")
	builder.WriteString(bc.UsedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(bc.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BackupCodes is a parsable slice of BackupCode.
type BackupCodes []*BackupCode
