// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/ordering-backend/internal/ent/datadeletionjob"
	"github.com/bengobox/ordering-backend/internal/ent/predicate"
	"github.com/google/uuid"
)

// DataDeletionJobQuery is the builder for querying DataDeletionJob entities.
type DataDeletionJobQuery struct {
	config
	ctx        *QueryContext
	order      []datadeletionjob.OrderOption
	inters     []Interceptor
	predicates []predicate.DataDeletionJob
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DataDeletionJobQuery builder.
func (ddjq *DataDeletionJobQuery) Where(ps ...predicate.DataDeletionJob) *DataDeletionJobQuery {
	ddjq.predicates = append(ddjq.predicates, ps...)
	return ddjq
}

// Limit the number of records to be returned by this query.
func (ddjq *DataDeletionJobQuery) Limit(limit int) *DataDeletionJobQuery {
	ddjq.ctx.Limit = &limit
	return ddjq
}

// Offset to start from.
func (ddjq *DataDeletionJobQuery) Offset(offset int) *DataDeletionJobQuery {
	ddjq.ctx.Offset = &offset
	return ddjq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ddjq *DataDeletionJobQuery) Unique(unique bool) *DataDeletionJobQuery {
	ddjq.ctx.Unique = &unique
	return ddjq
}

// Order specifies how the records should be ordered.
func (ddjq *DataDeletionJobQuery) Order(o ...datadeletionjob.OrderOption) *DataDeletionJobQuery {
	ddjq.order = append(ddjq.order, o...)
	return ddjq
}

// First returns the first DataDeletionJob entity from the query.
// Returns a *NotFoundError when no DataDeletionJob was found.
func (ddjq *DataDeletionJobQuery) First(ctx context.Context) (*DataDeletionJob, error) {
	nodes, err := ddjq.Limit(1).All(setContextOp(ctx, ddjq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{datadeletionjob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) FirstX(ctx context.Context) *DataDeletionJob {
	node, err := ddjq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DataDeletionJob ID from the query.
// Returns a *NotFoundError when no DataDeletionJob ID was found.
func (ddjq *DataDeletionJobQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ddjq.Limit(1).IDs(setContextOp(ctx, ddjq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{datadeletionjob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ddjq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DataDeletionJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DataDeletionJob entity is found.
// Returns a *NotFoundError when no DataDeletionJob entities are found.
func (ddjq *DataDeletionJobQuery) Only(ctx context.Context) (*DataDeletionJob, error) {
	nodes, err := ddjq.Limit(2).All(setContextOp(ctx, ddjq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{datadeletionjob.Label}
	default:
		return nil, &NotSingularError{datadeletionjob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) OnlyX(ctx context.Context) *DataDeletionJob {
	node, err := ddjq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DataDeletionJob ID in the query.
// Returns a *NotSingularError when more than one DataDeletionJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (ddjq *DataDeletionJobQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ddjq.Limit(2).IDs(setContextOp(ctx, ddjq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{datadeletionjob.Label}
	default:
		err = &NotSingularError{datadeletionjob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ddjq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DataDeletionJobs.
func (ddjq *DataDeletionJobQuery) All(ctx context.Context) ([]*DataDeletionJob, error) {
	ctx = setContextOp(ctx, ddjq.ctx, "All")
	if err := ddjq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DataDeletionJob, *DataDeletionJobQuery]()
	return withInterceptors[[]*DataDeletionJob](ctx, ddjq, qr, ddjq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) AllX(ctx context.Context) []*DataDeletionJob {
	nodes, err := ddjq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DataDeletionJob IDs.
func (ddjq *DataDeletionJobQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ddjq.ctx.Unique == nil && ddjq.path != nil {
		ddjq.Unique(true)
	}
	ctx = setContextOp(ctx, ddjq.ctx, "IDs")
	if err = ddjq.Select(datadeletionjob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ddjq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ddjq *DataDeletionJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ddjq.ctx, "Count")
	if err := ddjq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ddjq, querierCount[*DataDeletionJobQuery](), ddjq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) CountX(ctx context.Context) int {
	count, err := ddjq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ddjq *DataDeletionJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ddjq.ctx, "Exist")
	switch _, err := ddjq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ddjq *DataDeletionJobQuery) ExistX(ctx context.Context) bool {
	exist, err := ddjq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DataDeletionJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ddjq *DataDeletionJobQuery) Clone() *DataDeletionJobQuery {
	if ddjq == nil {
		return nil
	}
	return &DataDeletionJobQuery{
		config:     ddjq.config,
		ctx:        ddjq.ctx.Clone(),
		order:      append([]datadeletionjob.OrderOption{}, ddjq.order...),
		inters:     append([]Interceptor{}, ddjq.inters...),
		predicates: append([]predicate.DataDeletionJob{}, ddjq.predicates...),
		// clone intermediate query.
		sql:  ddjq.sql.Clone(),
		path: ddjq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DataDeletionJob.Query().
//		GroupBy(datadeletionjob.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ddjq *DataDeletionJobQuery) GroupBy(field string, fields ...string) *DataDeletionJobGroupBy {
	ddjq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DataDeletionJobGroupBy{build: ddjq}
	grbuild.flds = &ddjq.ctx.Fields
	grbuild.label = datadeletionjob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.DataDeletionJob.Query().
//		Select(datadeletionjob.FieldTenantID).
//		Scan(ctx, &v)
func (ddjq *DataDeletionJobQuery) Select(fields ...string) *DataDeletionJobSelect {
	ddjq.ctx.Fields = append(ddjq.ctx.Fields, fields...)
	sbuild := &DataDeletionJobSelect{DataDeletionJobQuery: ddjq}
	sbuild.label = datadeletionjob.Label
	sbuild.flds, sbuild.scan = &ddjq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DataDeletionJobSelect configured with the given aggregations.
func (ddjq *DataDeletionJobQuery) Aggregate(fns ...AggregateFunc) *DataDeletionJobSelect {
	return ddjq.Select().Aggregate(fns...)
}

func (ddjq *DataDeletionJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ddjq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ddjq); err != nil {
				return err
			}
		}
	}
	for _, f := range ddjq.ctx.Fields {
		if !datadeletionjob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ddjq.path != nil {
		prev, err := ddjq.path(ctx)
		if err != nil {
			return err
		}
		ddjq.sql = prev
	}
	return nil
}

func (ddjq *DataDeletionJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DataDeletionJob, error) {
	var (
		nodes = []*DataDeletionJob{}
		_spec = ddjq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DataDeletionJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DataDeletionJob{config: ddjq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ddjq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ddjq *DataDeletionJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ddjq.querySpec()
	_spec.Node.Columns = ddjq.ctx.Fields
	if len(ddjq.ctx.Fields) > 0 {
		_spec.Unique = ddjq.ctx.Unique != nil && *ddjq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ddjq.driver, _spec)
}

func (ddjq *DataDeletionJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(datadeletionjob.Table, datadeletionjob.Columns, sqlgraph.NewFieldSpec(datadeletionjob.FieldID, field.TypeUUID))
	_spec.From = ddjq.sql
	if unique := ddjq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ddjq.path != nil {
		_spec.Unique = true
	}
	if fields := ddjq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, datadeletionjob.FieldID)
		for i := range fields {
			if fields[i] != datadeletionjob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ddjq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ddjq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ddjq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ddjq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ddjq *DataDeletionJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ddjq.driver.Dialect())
	t1 := builder.Table(datadeletionjob.Table)
	columns := ddjq.ctx.Fields
	if len(columns) == 0 {
		columns = datadeletionjob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ddjq.sql != nil {
		selector = ddjq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ddjq.ctx.Unique != nil && *ddjq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ddjq.predicates {
		p(selector)
	}
	for _, p := range ddjq.order {
		p(selector)
	}
	if offset := ddjq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ddjq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DataDeletionJobGroupBy is the group-by builder for DataDeletionJob entities.
type DataDeletionJobGroupBy struct {
	selector
	build *DataDeletionJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ddjgb *DataDeletionJobGroupBy) Aggregate(fns ...AggregateFunc) *DataDeletionJobGroupBy {
	ddjgb.fns = append(ddjgb.fns, fns...)
	return ddjgb
}

// Scan applies the selector query and scans the result into the given value.
func (ddjgb *DataDeletionJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ddjgb.build.ctx, "GroupBy")
	if err := ddjgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DataDeletionJobQuery, *DataDeletionJobGroupBy](ctx, ddjgb.build, ddjgb, ddjgb.build.inters, v)
}

func (ddjgb *DataDeletionJobGroupBy) sqlScan(ctx context.Context, root *DataDeletionJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ddjgb.fns))
	for _, fn := range ddjgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ddjgb.flds)+len(ddjgb.fns))
		for _, f := range *ddjgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ddjgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ddjgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DataDeletionJobSelect is the builder for selecting fields of DataDeletionJob entities.
type DataDeletionJobSelect struct {
	*DataDeletionJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ddjs *DataDeletionJobSelect) Aggregate(fns ...AggregateFunc) *DataDeletionJobSelect {
	ddjs.fns = append(ddjs.fns, fns...)
	return ddjs
}

// Scan applies the selector query and scans the result into the given value.
func (ddjs *DataDeletionJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ddjs.ctx, "Select")
	if err := ddjs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DataDeletionJobQuery, *DataDeletionJobSelect](ctx, ddjs.DataDeletionJobQuery, ddjs, ddjs.inters, v)
}

func (ddjs *DataDeletionJobSelect) sqlScan(ctx context.Context, root *DataDeletionJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ddjs.fns))
	for _, fn := range ddjs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ddjs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ddjs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
