// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/catalogitem"
	"github.com/bengobox/ordering-backend/internal/ent/catalogitemasset"
	"github.com/google/uuid"
)

// CatalogItemAsset is the model entity for the CatalogItemAsset schema.
type CatalogItemAsset struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Reference to parent catalog item
	CatalogItemID uuid.UUID `json:"catalog_item_id,omitempty"`
	// Type: 'image', 'video', 'thumbnail'
	AssetType string `json:"asset_type,omitempty"`
	// CDN URL of the asset
	URL string `json:"url,omitempty"`
	// Additional metadata like dimensions, size, etc.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// DisplayOrder holds the value of the "display_order" field.
	DisplayOrder int `json:"display_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CatalogItemAssetQuery when eager-loading is set.
	Edges        CatalogItemAssetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CatalogItemAssetEdges holds the relations/edges for other nodes in the graph.
type CatalogItemAssetEdges struct {
	// CatalogItem holds the value of the catalog_item edge.
	CatalogItem *CatalogItem `json:"catalog_item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CatalogItemOrErr returns the CatalogItem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CatalogItemAssetEdges) CatalogItemOrErr() (*CatalogItem, error) {
	if e.CatalogItem != nil {
		return e.CatalogItem, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: catalogitem.Label}
	}
	return nil, &NotLoadedError{edge: "catalog_item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CatalogItemAsset) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case catalogitemasset.FieldMetadata:
			values[i] = new([]byte)
		case catalogitemasset.FieldDisplayOrder:
			values[i] = new(sql.NullInt64)
		case catalogitemasset.FieldAssetType, catalogitemasset.FieldURL:
			values[i] = new(sql.NullString)
		case catalogitemasset.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case catalogitemasset.FieldID, catalogitemasset.FieldCatalogItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CatalogItemAsset fields.
func (cia *CatalogItemAsset) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case catalogitemasset.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cia.ID = *value
			}
		case catalogitemasset.FieldCatalogItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field catalog_item_id", values[i])
			} else if value != nil {
				cia.CatalogItemID = *value
			}
		case catalogitemasset.FieldAssetType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field asset_type", values[i])
			} else if value.Valid {
				cia.AssetType = value.String
			}
		case catalogitemasset.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				cia.URL = value.String
			}
		case catalogitemasset.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &cia.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case catalogitemasset.FieldDisplayOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field display_order", values[i])
			} else if value.Valid {
				cia.DisplayOrder = int(value.Int64)
			}
		case catalogitemasset.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				cia.CreatedAt = value.Time
			}
		default:
			cia.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CatalogItemAsset.
// This includes values selected through modifiers, order, etc.
func (cia *CatalogItemAsset) Value(name string) (ent.Value, error) {
	return cia.selectValues.Get(name)
}

// QueryCatalogItem queries the "catalog_item" edge of the CatalogItemAsset entity.
func (cia *CatalogItemAsset) QueryCatalogItem() *CatalogItemQuery {
	return NewCatalogItemAssetClient(cia.config).QueryCatalogItem(cia)
}

// Update returns a builder for updating this CatalogItemAsset.
// Note that you need to call CatalogItemAsset.Unwrap() before calling this method if this CatalogItemAsset
// was returned from a transaction, and the transaction was committed or rolled back.
func (cia *CatalogItemAsset) Update() *CatalogItemAssetUpdateOne {
	return NewCatalogItemAssetClient(cia.config).UpdateOne(cia)
}

// Unwrap unwraps the CatalogItemAsset entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cia *CatalogItemAsset) Unwrap() *CatalogItemAsset {
	_tx, ok := cia.config.driver.(*txDriver)
	if !ok {
		panic("ent: CatalogItemAsset is not a transactional entity")
	}
	cia.config.driver = _tx.drv
	return cia
}

// String implements the fmt.Stringer.
func (cia *CatalogItemAsset) String() string {
	var builder strings.Builder
	builder.WriteString("CatalogItemAsset(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cia.ID))
	builder.WriteString("catalog_item_id=")
	builder.WriteString(fmt.Sprintf("%v", cia.CatalogItemID))
	builder.WriteString(", ")
	builder.WriteString("asset_type=")
	builder.WriteString(cia.AssetType)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(cia.URL)
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", cia.Metadata))
	builder.WriteString(", ")
	builder.WriteString("display_order=")
	builder.WriteString(fmt.Sprintf("%v", cia.DisplayOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(cia.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CatalogItemAssets is a parsable slice of CatalogItemAsset.
type CatalogItemAssets []*CatalogItemAsset
