// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/ordering-backend/internal/ent/menuitem"
	"github.com/bengobox/ordering-backend/internal/ent/menuitemtranslation"
	"github.com/bengobox/ordering-backend/internal/ent/predicate"
	"github.com/google/uuid"
)

// MenuItemTranslationQuery is the builder for querying MenuItemTranslation entities.
type MenuItemTranslationQuery struct {
	config
	ctx          *QueryContext
	order        []menuitemtranslation.OrderOption
	inters       []Interceptor
	predicates   []predicate.MenuItemTranslation
	withMenuItem *MenuItemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MenuItemTranslationQuery builder.
func (mitq *MenuItemTranslationQuery) Where(ps ...predicate.MenuItemTranslation) *MenuItemTranslationQuery {
	mitq.predicates = append(mitq.predicates, ps...)
	return mitq
}

// Limit the number of records to be returned by this query.
func (mitq *MenuItemTranslationQuery) Limit(limit int) *MenuItemTranslationQuery {
	mitq.ctx.Limit = &limit
	return mitq
}

// Offset to start from.
func (mitq *MenuItemTranslationQuery) Offset(offset int) *MenuItemTranslationQuery {
	mitq.ctx.Offset = &offset
	return mitq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mitq *MenuItemTranslationQuery) Unique(unique bool) *MenuItemTranslationQuery {
	mitq.ctx.Unique = &unique
	return mitq
}

// Order specifies how the records should be ordered.
func (mitq *MenuItemTranslationQuery) Order(o ...menuitemtranslation.OrderOption) *MenuItemTranslationQuery {
	mitq.order = append(mitq.order, o...)
	return mitq
}

// QueryMenuItem chains the current query on the "menu_item" edge.
func (mitq *MenuItemTranslationQuery) QueryMenuItem() *MenuItemQuery {
	query := (&MenuItemClient{config: mitq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(menuitemtranslation.Table, menuitemtranslation.FieldID, selector),
			sqlgraph.To(menuitem.Table, menuitem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, menuitemtranslation.MenuItemTable, menuitemtranslation.MenuItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(mitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MenuItemTranslation entity from the query.
// Returns a *NotFoundError when no MenuItemTranslation was found.
func (mitq *MenuItemTranslationQuery) First(ctx context.Context) (*MenuItemTranslation, error) {
	nodes, err := mitq.Limit(1).All(setContextOp(ctx, mitq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{menuitemtranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) FirstX(ctx context.Context) *MenuItemTranslation {
	node, err := mitq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MenuItemTranslation ID from the query.
// Returns a *NotFoundError when no MenuItemTranslation ID was found.
func (mitq *MenuItemTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mitq.Limit(1).IDs(setContextOp(ctx, mitq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{menuitemtranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := mitq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MenuItemTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MenuItemTranslation entity is found.
// Returns a *NotFoundError when no MenuItemTranslation entities are found.
func (mitq *MenuItemTranslationQuery) Only(ctx context.Context) (*MenuItemTranslation, error) {
	nodes, err := mitq.Limit(2).All(setContextOp(ctx, mitq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{menuitemtranslation.Label}
	default:
		return nil, &NotSingularError{menuitemtranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) OnlyX(ctx context.Context) *MenuItemTranslation {
	node, err := mitq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MenuItemTranslation ID in the query.
// Returns a *NotSingularError when more than one MenuItemTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (mitq *MenuItemTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mitq.Limit(2).IDs(setContextOp(ctx, mitq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{menuitemtranslation.Label}
	default:
		err = &NotSingularError{menuitemtranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := mitq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MenuItemTranslations.
func (mitq *MenuItemTranslationQuery) All(ctx context.Context) ([]*MenuItemTranslation, error) {
	ctx = setContextOp(ctx, mitq.ctx, "All")
	if err := mitq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MenuItemTranslation, *MenuItemTranslationQuery]()
	return withInterceptors[[]*MenuItemTranslation](ctx, mitq, qr, mitq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) AllX(ctx context.Context) []*MenuItemTranslation {
	nodes, err := mitq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MenuItemTranslation IDs.
func (mitq *MenuItemTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if mitq.ctx.Unique == nil && mitq.path != nil {
		mitq.Unique(true)
	}
	ctx = setContextOp(ctx, mitq.ctx, "IDs")
	if err = mitq.Select(menuitemtranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := mitq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mitq *MenuItemTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mitq.ctx, "Count")
	if err := mitq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mitq, querierCount[*MenuItemTranslationQuery](), mitq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) CountX(ctx context.Context) int {
	count, err := mitq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mitq *MenuItemTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mitq.ctx, "Exist")
	switch _, err := mitq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mitq *MenuItemTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := mitq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MenuItemTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mitq *MenuItemTranslationQuery) Clone() *MenuItemTranslationQuery {
	if mitq == nil {
		return nil
	}
	return &MenuItemTranslationQuery{
		config:       mitq.config,
		ctx:          mitq.ctx.Clone(),
		order:        append([]menuitemtranslation.OrderOption{}, mitq.order...),
		inters:       append([]Interceptor{}, mitq.inters...),
		predicates:   append([]predicate.MenuItemTranslation{}, mitq.predicates...),
		withMenuItem: mitq.withMenuItem.Clone(),
		// clone intermediate query.
		sql:  mitq.sql.Clone(),
		path: mitq.path,
	}
}

// WithMenuItem tells the query-builder to eager-load the nodes that are connected to
// the "menu_item" edge. The optional arguments are used to configure the query builder of the edge.
func (mitq *MenuItemTranslationQuery) WithMenuItem(opts ...func(*MenuItemQuery)) *MenuItemTranslationQuery {
	query := (&MenuItemClient{config: mitq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mitq.withMenuItem = query
	return mitq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MenuItemID uuid.UUID `json:"menu_item_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MenuItemTranslation.Query().
//		GroupBy(menuitemtranslation.FieldMenuItemID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mitq *MenuItemTranslationQuery) GroupBy(field string, fields ...string) *MenuItemTranslationGroupBy {
	mitq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MenuItemTranslationGroupBy{build: mitq}
	grbuild.flds = &mitq.ctx.Fields
	grbuild.label = menuitemtranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MenuItemID uuid.UUID `json:"menu_item_id,omitempty"`
//	}
//
//	client.MenuItemTranslation.Query().
//		Select(menuitemtranslation.FieldMenuItemID).
//		Scan(ctx, &v)
func (mitq *MenuItemTranslationQuery) Select(fields ...string) *MenuItemTranslationSelect {
	mitq.ctx.Fields = append(mitq.ctx.Fields, fields...)
	sbuild := &MenuItemTranslationSelect{MenuItemTranslationQuery: mitq}
	sbuild.label = menuitemtranslation.Label
	sbuild.flds, sbuild.scan = &mitq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MenuItemTranslationSelect configured with the given aggregations.
func (mitq *MenuItemTranslationQuery) Aggregate(fns ...AggregateFunc) *MenuItemTranslationSelect {
	return mitq.Select().Aggregate(fns...)
}

func (mitq *MenuItemTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mitq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mitq); err != nil {
				return err
			}
		}
	}
	for _, f := range mitq.ctx.Fields {
		if !menuitemtranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mitq.path != nil {
		prev, err := mitq.path(ctx)
		if err != nil {
			return err
		}
		mitq.sql = prev
	}
	return nil
}

func (mitq *MenuItemTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MenuItemTranslation, error) {
	var (
		nodes       = []*MenuItemTranslation{}
		_spec       = mitq.querySpec()
		loadedTypes = [1]bool{
			mitq.withMenuItem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MenuItemTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MenuItemTranslation{config: mitq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mitq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mitq.withMenuItem; query != nil {
		if err := mitq.loadMenuItem(ctx, query, nodes, nil,
			func(n *MenuItemTranslation, e *MenuItem) { n.Edges.MenuItem = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mitq *MenuItemTranslationQuery) loadMenuItem(ctx context.Context, query *MenuItemQuery, nodes []*MenuItemTranslation, init func(*MenuItemTranslation), assign func(*MenuItemTranslation, *MenuItem)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*MenuItemTranslation)
	for i := range nodes {
		fk := nodes[i].MenuItemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(menuitem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "menu_item_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (mitq *MenuItemTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mitq.querySpec()
	_spec.Node.Columns = mitq.ctx.Fields
	if len(mitq.ctx.Fields) > 0 {
		_spec.Unique = mitq.ctx.Unique != nil && *mitq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mitq.driver, _spec)
}

func (mitq *MenuItemTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(menuitemtranslation.Table, menuitemtranslation.Columns, sqlgraph.NewFieldSpec(menuitemtranslation.FieldID, field.TypeUUID))
	_spec.From = mitq.sql
	if unique := mitq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mitq.path != nil {
		_spec.Unique = true
	}
	if fields := mitq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, menuitemtranslation.FieldID)
		for i := range fields {
			if fields[i] != menuitemtranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if mitq.withMenuItem != nil {
			_spec.Node.AddColumnOnce(menuitemtranslation.FieldMenuItemID)
		}
	}
	if ps := mitq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mitq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mitq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mitq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mitq *MenuItemTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mitq.driver.Dialect())
	t1 := builder.Table(menuitemtranslation.Table)
	columns := mitq.ctx.Fields
	if len(columns) == 0 {
		columns = menuitemtranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mitq.sql != nil {
		selector = mitq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mitq.ctx.Unique != nil && *mitq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mitq.predicates {
		p(selector)
	}
	for _, p := range mitq.order {
		p(selector)
	}
	if offset := mitq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mitq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MenuItemTranslationGroupBy is the group-by builder for MenuItemTranslation entities.
type MenuItemTranslationGroupBy struct {
	selector
	build *MenuItemTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mitgb *MenuItemTranslationGroupBy) Aggregate(fns ...AggregateFunc) *MenuItemTranslationGroupBy {
	mitgb.fns = append(mitgb.fns, fns...)
	return mitgb
}

// Scan applies the selector query and scans the result into the given value.
func (mitgb *MenuItemTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mitgb.build.ctx, "GroupBy")
	if err := mitgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MenuItemTranslationQuery, *MenuItemTranslationGroupBy](ctx, mitgb.build, mitgb, mitgb.build.inters, v)
}

func (mitgb *MenuItemTranslationGroupBy) sqlScan(ctx context.Context, root *MenuItemTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mitgb.fns))
	for _, fn := range mitgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mitgb.flds)+len(mitgb.fns))
		for _, f := range *mitgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mitgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mitgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MenuItemTranslationSelect is the builder for selecting fields of MenuItemTranslation entities.
type MenuItemTranslationSelect struct {
	*MenuItemTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mits *MenuItemTranslationSelect) Aggregate(fns ...AggregateFunc) *MenuItemTranslationSelect {
	mits.fns = append(mits.fns, fns...)
	return mits
}

// Scan applies the selector query and scans the result into the given value.
func (mits *MenuItemTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mits.ctx, "Select")
	if err := mits.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MenuItemTranslationQuery, *MenuItemTranslationSelect](ctx, mits.MenuItemTranslationQuery, mits, mits.inters, v)
}

func (mits *MenuItemTranslationSelect) sqlScan(ctx context.Context, root *MenuItemTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mits.fns))
	for _, fn := range mits.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mits.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mits.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
