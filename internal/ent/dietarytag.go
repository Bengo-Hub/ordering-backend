// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/dietarytag"
)

// DietaryTag is the model entity for the DietaryTag schema.
type DietaryTag struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Unique code, e.g., 'vegetarian', 'vegan', 'gluten-free'
	Code string `json:"code,omitempty"`
	// Display label
	Label string `json:"label,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// IconURL holds the value of the "icon_url" field.
	IconURL string `json:"icon_url,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DietaryTagQuery when eager-loading is set.
	Edges        DietaryTagEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DietaryTagEdges holds the relations/edges for other nodes in the graph.
type DietaryTagEdges struct {
	// MenuItems holds the value of the menu_items edge.
	MenuItems []*MenuItem `json:"menu_items,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MenuItemsOrErr returns the MenuItems value or an error if the edge
// was not loaded in eager-loading.
func (e DietaryTagEdges) MenuItemsOrErr() ([]*MenuItem, error) {
	if e.loadedTypes[0] {
		return e.MenuItems, nil
	}
	return nil, &NotLoadedError{edge: "menu_items"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DietaryTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dietarytag.FieldID:
			values[i] = new(sql.NullInt64)
		case dietarytag.FieldCode, dietarytag.FieldLabel, dietarytag.FieldDescription, dietarytag.FieldIconURL:
			values[i] = new(sql.NullString)
		case dietarytag.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DietaryTag fields.
func (dt *DietaryTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dietarytag.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dt.ID = int(value.Int64)
		case dietarytag.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				dt.Code = value.String
			}
		case dietarytag.FieldLabel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value.Valid {
				dt.Label = value.String
			}
		case dietarytag.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				dt.Description = value.String
			}
		case dietarytag.FieldIconURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field icon_url", values[i])
			} else if value.Valid {
				dt.IconURL = value.String
			}
		case dietarytag.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dt.CreatedAt = value.Time
			}
		default:
			dt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DietaryTag.
// This includes values selected through modifiers, order, etc.
func (dt *DietaryTag) Value(name string) (ent.Value, error) {
	return dt.selectValues.Get(name)
}

// QueryMenuItems queries the "menu_items" edge of the DietaryTag entity.
func (dt *DietaryTag) QueryMenuItems() *MenuItemQuery {
	return NewDietaryTagClient(dt.config).QueryMenuItems(dt)
}

// Update returns a builder for updating this DietaryTag.
// Note that you need to call DietaryTag.Unwrap() before calling this method if this DietaryTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (dt *DietaryTag) Update() *DietaryTagUpdateOne {
	return NewDietaryTagClient(dt.config).UpdateOne(dt)
}

// Unwrap unwraps the DietaryTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dt *DietaryTag) Unwrap() *DietaryTag {
	_tx, ok := dt.config.driver.(*txDriver)
	if !ok {
		panic("ent: DietaryTag is not a transactional entity")
	}
	dt.config.driver = _tx.drv
	return dt
}

// String implements the fmt.Stringer.
func (dt *DietaryTag) String() string {
	var builder strings.Builder
	builder.WriteString("DietaryTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dt.ID))
	builder.WriteString("code=")
	builder.WriteString(dt.Code)
	builder.WriteString(", ")
	builder.WriteString("label=")
	builder.WriteString(dt.Label)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(dt.Description)
	builder.WriteString(", ")
	builder.WriteString("icon_url=")
	builder.WriteString(dt.IconURL)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dt.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DietaryTags is a parsable slice of DietaryTag.
type DietaryTags []*DietaryTag
