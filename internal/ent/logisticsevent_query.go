// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/ordering-backend/internal/ent/logisticsevent"
	"github.com/bengobox/ordering-backend/internal/ent/predicate"
	"github.com/google/uuid"
)

// LogisticsEventQuery is the builder for querying LogisticsEvent entities.
type LogisticsEventQuery struct {
	config
	ctx        *QueryContext
	order      []logisticsevent.OrderOption
	inters     []Interceptor
	predicates []predicate.LogisticsEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LogisticsEventQuery builder.
func (leq *LogisticsEventQuery) Where(ps ...predicate.LogisticsEvent) *LogisticsEventQuery {
	leq.predicates = append(leq.predicates, ps...)
	return leq
}

// Limit the number of records to be returned by this query.
func (leq *LogisticsEventQuery) Limit(limit int) *LogisticsEventQuery {
	leq.ctx.Limit = &limit
	return leq
}

// Offset to start from.
func (leq *LogisticsEventQuery) Offset(offset int) *LogisticsEventQuery {
	leq.ctx.Offset = &offset
	return leq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (leq *LogisticsEventQuery) Unique(unique bool) *LogisticsEventQuery {
	leq.ctx.Unique = &unique
	return leq
}

// Order specifies how the records should be ordered.
func (leq *LogisticsEventQuery) Order(o ...logisticsevent.OrderOption) *LogisticsEventQuery {
	leq.order = append(leq.order, o...)
	return leq
}

// First returns the first LogisticsEvent entity from the query.
// Returns a *NotFoundError when no LogisticsEvent was found.
func (leq *LogisticsEventQuery) First(ctx context.Context) (*LogisticsEvent, error) {
	nodes, err := leq.Limit(1).All(setContextOp(ctx, leq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{logisticsevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (leq *LogisticsEventQuery) FirstX(ctx context.Context) *LogisticsEvent {
	node, err := leq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LogisticsEvent ID from the query.
// Returns a *NotFoundError when no LogisticsEvent ID was found.
func (leq *LogisticsEventQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = leq.Limit(1).IDs(setContextOp(ctx, leq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{logisticsevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (leq *LogisticsEventQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := leq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LogisticsEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LogisticsEvent entity is found.
// Returns a *NotFoundError when no LogisticsEvent entities are found.
func (leq *LogisticsEventQuery) Only(ctx context.Context) (*LogisticsEvent, error) {
	nodes, err := leq.Limit(2).All(setContextOp(ctx, leq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{logisticsevent.Label}
	default:
		return nil, &NotSingularError{logisticsevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (leq *LogisticsEventQuery) OnlyX(ctx context.Context) *LogisticsEvent {
	node, err := leq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LogisticsEvent ID in the query.
// Returns a *NotSingularError when more than one LogisticsEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (leq *LogisticsEventQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = leq.Limit(2).IDs(setContextOp(ctx, leq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{logisticsevent.Label}
	default:
		err = &NotSingularError{logisticsevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (leq *LogisticsEventQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := leq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LogisticsEvents.
func (leq *LogisticsEventQuery) All(ctx context.Context) ([]*LogisticsEvent, error) {
	ctx = setContextOp(ctx, leq.ctx, "All")
	if err := leq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LogisticsEvent, *LogisticsEventQuery]()
	return withInterceptors[[]*LogisticsEvent](ctx, leq, qr, leq.inters)
}

// AllX is like All, but panics if an error occurs.
func (leq *LogisticsEventQuery) AllX(ctx context.Context) []*LogisticsEvent {
	nodes, err := leq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LogisticsEvent IDs.
func (leq *LogisticsEventQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if leq.ctx.Unique == nil && leq.path != nil {
		leq.Unique(true)
	}
	ctx = setContextOp(ctx, leq.ctx, "IDs")
	if err = leq.Select(logisticsevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (leq *LogisticsEventQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := leq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (leq *LogisticsEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, leq.ctx, "Count")
	if err := leq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, leq, querierCount[*LogisticsEventQuery](), leq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (leq *LogisticsEventQuery) CountX(ctx context.Context) int {
	count, err := leq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (leq *LogisticsEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, leq.ctx, "Exist")
	switch _, err := leq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (leq *LogisticsEventQuery) ExistX(ctx context.Context) bool {
	exist, err := leq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LogisticsEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (leq *LogisticsEventQuery) Clone() *LogisticsEventQuery {
	if leq == nil {
		return nil
	}
	return &LogisticsEventQuery{
		config:     leq.config,
		ctx:        leq.ctx.Clone(),
		order:      append([]logisticsevent.OrderOption{}, leq.order...),
		inters:     append([]Interceptor{}, leq.inters...),
		predicates: append([]predicate.LogisticsEvent{}, leq.predicates...),
		// clone intermediate query.
		sql:  leq.sql.Clone(),
		path: leq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LogisticsEvent.Query().
//		GroupBy(logisticsevent.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (leq *LogisticsEventQuery) GroupBy(field string, fields ...string) *LogisticsEventGroupBy {
	leq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LogisticsEventGroupBy{build: leq}
	grbuild.flds = &leq.ctx.Fields
	grbuild.label = logisticsevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.LogisticsEvent.Query().
//		Select(logisticsevent.FieldTenantID).
//		Scan(ctx, &v)
func (leq *LogisticsEventQuery) Select(fields ...string) *LogisticsEventSelect {
	leq.ctx.Fields = append(leq.ctx.Fields, fields...)
	sbuild := &LogisticsEventSelect{LogisticsEventQuery: leq}
	sbuild.label = logisticsevent.Label
	sbuild.flds, sbuild.scan = &leq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LogisticsEventSelect configured with the given aggregations.
func (leq *LogisticsEventQuery) Aggregate(fns ...AggregateFunc) *LogisticsEventSelect {
	return leq.Select().Aggregate(fns...)
}

func (leq *LogisticsEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range leq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, leq); err != nil {
				return err
			}
		}
	}
	for _, f := range leq.ctx.Fields {
		if !logisticsevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if leq.path != nil {
		prev, err := leq.path(ctx)
		if err != nil {
			return err
		}
		leq.sql = prev
	}
	return nil
}

func (leq *LogisticsEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LogisticsEvent, error) {
	var (
		nodes = []*LogisticsEvent{}
		_spec = leq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LogisticsEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LogisticsEvent{config: leq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, leq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (leq *LogisticsEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := leq.querySpec()
	_spec.Node.Columns = leq.ctx.Fields
	if len(leq.ctx.Fields) > 0 {
		_spec.Unique = leq.ctx.Unique != nil && *leq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, leq.driver, _spec)
}

func (leq *LogisticsEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(logisticsevent.Table, logisticsevent.Columns, sqlgraph.NewFieldSpec(logisticsevent.FieldID, field.TypeUUID))
	_spec.From = leq.sql
	if unique := leq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if leq.path != nil {
		_spec.Unique = true
	}
	if fields := leq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, logisticsevent.FieldID)
		for i := range fields {
			if fields[i] != logisticsevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := leq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := leq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := leq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := leq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (leq *LogisticsEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(leq.driver.Dialect())
	t1 := builder.Table(logisticsevent.Table)
	columns := leq.ctx.Fields
	if len(columns) == 0 {
		columns = logisticsevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if leq.sql != nil {
		selector = leq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if leq.ctx.Unique != nil && *leq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range leq.predicates {
		p(selector)
	}
	for _, p := range leq.order {
		p(selector)
	}
	if offset := leq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := leq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LogisticsEventGroupBy is the group-by builder for LogisticsEvent entities.
type LogisticsEventGroupBy struct {
	selector
	build *LogisticsEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (legb *LogisticsEventGroupBy) Aggregate(fns ...AggregateFunc) *LogisticsEventGroupBy {
	legb.fns = append(legb.fns, fns...)
	return legb
}

// Scan applies the selector query and scans the result into the given value.
func (legb *LogisticsEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, legb.build.ctx, "GroupBy")
	if err := legb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LogisticsEventQuery, *LogisticsEventGroupBy](ctx, legb.build, legb, legb.build.inters, v)
}

func (legb *LogisticsEventGroupBy) sqlScan(ctx context.Context, root *LogisticsEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(legb.fns))
	for _, fn := range legb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*legb.flds)+len(legb.fns))
		for _, f := range *legb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*legb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := legb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LogisticsEventSelect is the builder for selecting fields of LogisticsEvent entities.
type LogisticsEventSelect struct {
	*LogisticsEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (les *LogisticsEventSelect) Aggregate(fns ...AggregateFunc) *LogisticsEventSelect {
	les.fns = append(les.fns, fns...)
	return les
}

// Scan applies the selector query and scans the result into the given value.
func (les *LogisticsEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, les.ctx, "Select")
	if err := les.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LogisticsEventQuery, *LogisticsEventSelect](ctx, les.LogisticsEventQuery, les, les.inters, v)
}

func (les *LogisticsEventSelect) sqlScan(ctx context.Context, root *LogisticsEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(les.fns))
	for _, fn := range les.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*les.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := les.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
