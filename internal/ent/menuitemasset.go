// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/menuitem"
	"github.com/bengobox/ordering-backend/internal/ent/menuitemasset"
	"github.com/google/uuid"
)

// MenuItemAsset is the model entity for the MenuItemAsset schema.
type MenuItemAsset struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Reference to parent menu item
	MenuItemID uuid.UUID `json:"menu_item_id,omitempty"`
	// Type: 'image', 'video', 'thumbnail'
	AssetType string `json:"asset_type,omitempty"`
	// CDN URL of the asset
	URL string `json:"url,omitempty"`
	// Additional metadata like dimensions, size, etc.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// DisplayOrder holds the value of the "display_order" field.
	DisplayOrder int `json:"display_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MenuItemAssetQuery when eager-loading is set.
	Edges        MenuItemAssetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MenuItemAssetEdges holds the relations/edges for other nodes in the graph.
type MenuItemAssetEdges struct {
	// MenuItem holds the value of the menu_item edge.
	MenuItem *MenuItem `json:"menu_item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MenuItemOrErr returns the MenuItem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MenuItemAssetEdges) MenuItemOrErr() (*MenuItem, error) {
	if e.MenuItem != nil {
		return e.MenuItem, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: menuitem.Label}
	}
	return nil, &NotLoadedError{edge: "menu_item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MenuItemAsset) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case menuitemasset.FieldMetadata:
			values[i] = new([]byte)
		case menuitemasset.FieldDisplayOrder:
			values[i] = new(sql.NullInt64)
		case menuitemasset.FieldAssetType, menuitemasset.FieldURL:
			values[i] = new(sql.NullString)
		case menuitemasset.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case menuitemasset.FieldID, menuitemasset.FieldMenuItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MenuItemAsset fields.
func (mia *MenuItemAsset) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case menuitemasset.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				mia.ID = *value
			}
		case menuitemasset.FieldMenuItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field menu_item_id", values[i])
			} else if value != nil {
				mia.MenuItemID = *value
			}
		case menuitemasset.FieldAssetType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field asset_type", values[i])
			} else if value.Valid {
				mia.AssetType = value.String
			}
		case menuitemasset.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				mia.URL = value.String
			}
		case menuitemasset.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &mia.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case menuitemasset.FieldDisplayOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field display_order", values[i])
			} else if value.Valid {
				mia.DisplayOrder = int(value.Int64)
			}
		case menuitemasset.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				mia.CreatedAt = value.Time
			}
		default:
			mia.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MenuItemAsset.
// This includes values selected through modifiers, order, etc.
func (mia *MenuItemAsset) Value(name string) (ent.Value, error) {
	return mia.selectValues.Get(name)
}

// QueryMenuItem queries the "menu_item" edge of the MenuItemAsset entity.
func (mia *MenuItemAsset) QueryMenuItem() *MenuItemQuery {
	return NewMenuItemAssetClient(mia.config).QueryMenuItem(mia)
}

// Update returns a builder for updating this MenuItemAsset.
// Note that you need to call MenuItemAsset.Unwrap() before calling this method if this MenuItemAsset
// was returned from a transaction, and the transaction was committed or rolled back.
func (mia *MenuItemAsset) Update() *MenuItemAssetUpdateOne {
	return NewMenuItemAssetClient(mia.config).UpdateOne(mia)
}

// Unwrap unwraps the MenuItemAsset entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mia *MenuItemAsset) Unwrap() *MenuItemAsset {
	_tx, ok := mia.config.driver.(*txDriver)
	if !ok {
		panic("ent: MenuItemAsset is not a transactional entity")
	}
	mia.config.driver = _tx.drv
	return mia
}

// String implements the fmt.Stringer.
func (mia *MenuItemAsset) String() string {
	var builder strings.Builder
	builder.WriteString("MenuItemAsset(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mia.ID))
	builder.WriteString("menu_item_id=")
	builder.WriteString(fmt.Sprintf("%v", mia.MenuItemID))
	builder.WriteString(", ")
	builder.WriteString("asset_type=")
	builder.WriteString(mia.AssetType)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(mia.URL)
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", mia.Metadata))
	builder.WriteString(", ")
	builder.WriteString("display_order=")
	builder.WriteString(fmt.Sprintf("%v", mia.DisplayOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(mia.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MenuItemAssets is a parsable slice of MenuItemAsset.
type MenuItemAssets []*MenuItemAsset
