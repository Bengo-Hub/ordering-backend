// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/catalogitem"
	"github.com/bengobox/ordering-backend/internal/ent/catalogitemschedule"
	"github.com/google/uuid"
)

// CatalogItemSchedule is the model entity for the CatalogItemSchedule schema.
type CatalogItemSchedule struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Reference to parent catalog item
	CatalogItemID uuid.UUID `json:"catalog_item_id,omitempty"`
	// Day of week: 0=Sunday, 6=Saturday
	DayOfWeek int `json:"day_of_week,omitempty"`
	// Start time in HH:MM format
	TimeStart string `json:"time_start,omitempty"`
	// End time in HH:MM format
	TimeEnd string `json:"time_end,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CatalogItemScheduleQuery when eager-loading is set.
	Edges        CatalogItemScheduleEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CatalogItemScheduleEdges holds the relations/edges for other nodes in the graph.
type CatalogItemScheduleEdges struct {
	// CatalogItem holds the value of the catalog_item edge.
	CatalogItem *CatalogItem `json:"catalog_item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CatalogItemOrErr returns the CatalogItem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CatalogItemScheduleEdges) CatalogItemOrErr() (*CatalogItem, error) {
	if e.CatalogItem != nil {
		return e.CatalogItem, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: catalogitem.Label}
	}
	return nil, &NotLoadedError{edge: "catalog_item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CatalogItemSchedule) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case catalogitemschedule.FieldDayOfWeek:
			values[i] = new(sql.NullInt64)
		case catalogitemschedule.FieldTimeStart, catalogitemschedule.FieldTimeEnd:
			values[i] = new(sql.NullString)
		case catalogitemschedule.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case catalogitemschedule.FieldID, catalogitemschedule.FieldCatalogItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CatalogItemSchedule fields.
func (cis *CatalogItemSchedule) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case catalogitemschedule.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				cis.ID = *value
			}
		case catalogitemschedule.FieldCatalogItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field catalog_item_id", values[i])
			} else if value != nil {
				cis.CatalogItemID = *value
			}
		case catalogitemschedule.FieldDayOfWeek:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field day_of_week", values[i])
			} else if value.Valid {
				cis.DayOfWeek = int(value.Int64)
			}
		case catalogitemschedule.FieldTimeStart:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field time_start", values[i])
			} else if value.Valid {
				cis.TimeStart = value.String
			}
		case catalogitemschedule.FieldTimeEnd:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field time_end", values[i])
			} else if value.Valid {
				cis.TimeEnd = value.String
			}
		case catalogitemschedule.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				cis.CreatedAt = value.Time
			}
		default:
			cis.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CatalogItemSchedule.
// This includes values selected through modifiers, order, etc.
func (cis *CatalogItemSchedule) Value(name string) (ent.Value, error) {
	return cis.selectValues.Get(name)
}

// QueryCatalogItem queries the "catalog_item" edge of the CatalogItemSchedule entity.
func (cis *CatalogItemSchedule) QueryCatalogItem() *CatalogItemQuery {
	return NewCatalogItemScheduleClient(cis.config).QueryCatalogItem(cis)
}

// Update returns a builder for updating this CatalogItemSchedule.
// Note that you need to call CatalogItemSchedule.Unwrap() before calling this method if this CatalogItemSchedule
// was returned from a transaction, and the transaction was committed or rolled back.
func (cis *CatalogItemSchedule) Update() *CatalogItemScheduleUpdateOne {
	return NewCatalogItemScheduleClient(cis.config).UpdateOne(cis)
}

// Unwrap unwraps the CatalogItemSchedule entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cis *CatalogItemSchedule) Unwrap() *CatalogItemSchedule {
	_tx, ok := cis.config.driver.(*txDriver)
	if !ok {
		panic("ent: CatalogItemSchedule is not a transactional entity")
	}
	cis.config.driver = _tx.drv
	return cis
}

// String implements the fmt.Stringer.
func (cis *CatalogItemSchedule) String() string {
	var builder strings.Builder
	builder.WriteString("CatalogItemSchedule(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cis.ID))
	builder.WriteString("catalog_item_id=")
	builder.WriteString(fmt.Sprintf("%v", cis.CatalogItemID))
	builder.WriteString(", ")
	builder.WriteString("day_of_week=")
	builder.WriteString(fmt.Sprintf("%v", cis.DayOfWeek))
	builder.WriteString(", ")
	builder.WriteString("time_start=")
	builder.WriteString(cis.TimeStart)
	builder.WriteString(", ")
	builder.WriteString("time_end=")
	builder.WriteString(cis.TimeEnd)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(cis.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CatalogItemSchedules is a parsable slice of CatalogItemSchedule.
type CatalogItemSchedules []*CatalogItemSchedule
