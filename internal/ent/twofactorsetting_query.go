// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/food-delivery-backend/internal/ent/predicate"
	"github.com/bengobox/food-delivery-backend/internal/ent/twofactorsetting"
	"github.com/bengobox/food-delivery-backend/internal/ent/user"
)

// TwoFactorSettingQuery is the builder for querying TwoFactorSetting entities.
type TwoFactorSettingQuery struct {
	config
	ctx        *QueryContext
	order      []twofactorsetting.OrderOption
	inters     []Interceptor
	predicates []predicate.TwoFactorSetting
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TwoFactorSettingQuery builder.
func (tfsq *TwoFactorSettingQuery) Where(ps ...predicate.TwoFactorSetting) *TwoFactorSettingQuery {
	tfsq.predicates = append(tfsq.predicates, ps...)
	return tfsq
}

// Limit the number of records to be returned by this query.
func (tfsq *TwoFactorSettingQuery) Limit(limit int) *TwoFactorSettingQuery {
	tfsq.ctx.Limit = &limit
	return tfsq
}

// Offset to start from.
func (tfsq *TwoFactorSettingQuery) Offset(offset int) *TwoFactorSettingQuery {
	tfsq.ctx.Offset = &offset
	return tfsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tfsq *TwoFactorSettingQuery) Unique(unique bool) *TwoFactorSettingQuery {
	tfsq.ctx.Unique = &unique
	return tfsq
}

// Order specifies how the records should be ordered.
func (tfsq *TwoFactorSettingQuery) Order(o ...twofactorsetting.OrderOption) *TwoFactorSettingQuery {
	tfsq.order = append(tfsq.order, o...)
	return tfsq
}

// QueryUser chains the current query on the "user" edge.
func (tfsq *TwoFactorSettingQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: tfsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tfsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tfsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(twofactorsetting.Table, twofactorsetting.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, twofactorsetting.UserTable, twofactorsetting.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(tfsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TwoFactorSetting entity from the query.
// Returns a *NotFoundError when no TwoFactorSetting was found.
func (tfsq *TwoFactorSettingQuery) First(ctx context.Context) (*TwoFactorSetting, error) {
	nodes, err := tfsq.Limit(1).All(setContextOp(ctx, tfsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{twofactorsetting.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) FirstX(ctx context.Context) *TwoFactorSetting {
	node, err := tfsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TwoFactorSetting ID from the query.
// Returns a *NotFoundError when no TwoFactorSetting ID was found.
func (tfsq *TwoFactorSettingQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tfsq.Limit(1).IDs(setContextOp(ctx, tfsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{twofactorsetting.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) FirstIDX(ctx context.Context) int {
	id, err := tfsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TwoFactorSetting entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TwoFactorSetting entity is found.
// Returns a *NotFoundError when no TwoFactorSetting entities are found.
func (tfsq *TwoFactorSettingQuery) Only(ctx context.Context) (*TwoFactorSetting, error) {
	nodes, err := tfsq.Limit(2).All(setContextOp(ctx, tfsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{twofactorsetting.Label}
	default:
		return nil, &NotSingularError{twofactorsetting.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) OnlyX(ctx context.Context) *TwoFactorSetting {
	node, err := tfsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TwoFactorSetting ID in the query.
// Returns a *NotSingularError when more than one TwoFactorSetting ID is found.
// Returns a *NotFoundError when no entities are found.
func (tfsq *TwoFactorSettingQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tfsq.Limit(2).IDs(setContextOp(ctx, tfsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{twofactorsetting.Label}
	default:
		err = &NotSingularError{twofactorsetting.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) OnlyIDX(ctx context.Context) int {
	id, err := tfsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TwoFactorSettings.
func (tfsq *TwoFactorSettingQuery) All(ctx context.Context) ([]*TwoFactorSetting, error) {
	ctx = setContextOp(ctx, tfsq.ctx, "All")
	if err := tfsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TwoFactorSetting, *TwoFactorSettingQuery]()
	return withInterceptors[[]*TwoFactorSetting](ctx, tfsq, qr, tfsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) AllX(ctx context.Context) []*TwoFactorSetting {
	nodes, err := tfsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TwoFactorSetting IDs.
func (tfsq *TwoFactorSettingQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tfsq.ctx.Unique == nil && tfsq.path != nil {
		tfsq.Unique(true)
	}
	ctx = setContextOp(ctx, tfsq.ctx, "IDs")
	if err = tfsq.Select(twofactorsetting.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) IDsX(ctx context.Context) []int {
	ids, err := tfsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tfsq *TwoFactorSettingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tfsq.ctx, "Count")
	if err := tfsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tfsq, querierCount[*TwoFactorSettingQuery](), tfsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) CountX(ctx context.Context) int {
	count, err := tfsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tfsq *TwoFactorSettingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tfsq.ctx, "Exist")
	switch _, err := tfsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tfsq *TwoFactorSettingQuery) ExistX(ctx context.Context) bool {
	exist, err := tfsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TwoFactorSettingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tfsq *TwoFactorSettingQuery) Clone() *TwoFactorSettingQuery {
	if tfsq == nil {
		return nil
	}
	return &TwoFactorSettingQuery{
		config:     tfsq.config,
		ctx:        tfsq.ctx.Clone(),
		order:      append([]twofactorsetting.OrderOption{}, tfsq.order...),
		inters:     append([]Interceptor{}, tfsq.inters...),
		predicates: append([]predicate.TwoFactorSetting{}, tfsq.predicates...),
		withUser:   tfsq.withUser.Clone(),
		// clone intermediate query.
		sql:  tfsq.sql.Clone(),
		path: tfsq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (tfsq *TwoFactorSettingQuery) WithUser(opts ...func(*UserQuery)) *TwoFactorSettingQuery {
	query := (&UserClient{config: tfsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tfsq.withUser = query
	return tfsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Method string `json:"method,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TwoFactorSetting.Query().
//		GroupBy(twofactorsetting.FieldMethod).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tfsq *TwoFactorSettingQuery) GroupBy(field string, fields ...string) *TwoFactorSettingGroupBy {
	tfsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TwoFactorSettingGroupBy{build: tfsq}
	grbuild.flds = &tfsq.ctx.Fields
	grbuild.label = twofactorsetting.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Method string `json:"method,omitempty"`
//	}
//
//	client.TwoFactorSetting.Query().
//		Select(twofactorsetting.FieldMethod).
//		Scan(ctx, &v)
func (tfsq *TwoFactorSettingQuery) Select(fields ...string) *TwoFactorSettingSelect {
	tfsq.ctx.Fields = append(tfsq.ctx.Fields, fields...)
	sbuild := &TwoFactorSettingSelect{TwoFactorSettingQuery: tfsq}
	sbuild.label = twofactorsetting.Label
	sbuild.flds, sbuild.scan = &tfsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TwoFactorSettingSelect configured with the given aggregations.
func (tfsq *TwoFactorSettingQuery) Aggregate(fns ...AggregateFunc) *TwoFactorSettingSelect {
	return tfsq.Select().Aggregate(fns...)
}

func (tfsq *TwoFactorSettingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tfsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tfsq); err != nil {
				return err
			}
		}
	}
	for _, f := range tfsq.ctx.Fields {
		if !twofactorsetting.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tfsq.path != nil {
		prev, err := tfsq.path(ctx)
		if err != nil {
			return err
		}
		tfsq.sql = prev
	}
	return nil
}

func (tfsq *TwoFactorSettingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TwoFactorSetting, error) {
	var (
		nodes       = []*TwoFactorSetting{}
		_spec       = tfsq.querySpec()
		loadedTypes = [1]bool{
			tfsq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TwoFactorSetting).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TwoFactorSetting{config: tfsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tfsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tfsq.withUser; query != nil {
		if err := tfsq.loadUser(ctx, query, nodes, nil,
			func(n *TwoFactorSetting, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tfsq *TwoFactorSettingQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*TwoFactorSetting, init func(*TwoFactorSetting), assign func(*TwoFactorSetting, *User)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*TwoFactorSetting)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.User(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(twofactorsetting.UserColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.two_factor_setting_user
		if fk == nil {
			return fmt.Errorf(`foreign-key "two_factor_setting_user" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "two_factor_setting_user" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (tfsq *TwoFactorSettingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tfsq.querySpec()
	_spec.Node.Columns = tfsq.ctx.Fields
	if len(tfsq.ctx.Fields) > 0 {
		_spec.Unique = tfsq.ctx.Unique != nil && *tfsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tfsq.driver, _spec)
}

func (tfsq *TwoFactorSettingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(twofactorsetting.Table, twofactorsetting.Columns, sqlgraph.NewFieldSpec(twofactorsetting.FieldID, field.TypeInt))
	_spec.From = tfsq.sql
	if unique := tfsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tfsq.path != nil {
		_spec.Unique = true
	}
	if fields := tfsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, twofactorsetting.FieldID)
		for i := range fields {
			if fields[i] != twofactorsetting.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tfsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tfsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tfsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tfsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tfsq *TwoFactorSettingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tfsq.driver.Dialect())
	t1 := builder.Table(twofactorsetting.Table)
	columns := tfsq.ctx.Fields
	if len(columns) == 0 {
		columns = twofactorsetting.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tfsq.sql != nil {
		selector = tfsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tfsq.ctx.Unique != nil && *tfsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tfsq.predicates {
		p(selector)
	}
	for _, p := range tfsq.order {
		p(selector)
	}
	if offset := tfsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tfsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TwoFactorSettingGroupBy is the group-by builder for TwoFactorSetting entities.
type TwoFactorSettingGroupBy struct {
	selector
	build *TwoFactorSettingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tfsgb *TwoFactorSettingGroupBy) Aggregate(fns ...AggregateFunc) *TwoFactorSettingGroupBy {
	tfsgb.fns = append(tfsgb.fns, fns...)
	return tfsgb
}

// Scan applies the selector query and scans the result into the given value.
func (tfsgb *TwoFactorSettingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tfsgb.build.ctx, "GroupBy")
	if err := tfsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TwoFactorSettingQuery, *TwoFactorSettingGroupBy](ctx, tfsgb.build, tfsgb, tfsgb.build.inters, v)
}

func (tfsgb *TwoFactorSettingGroupBy) sqlScan(ctx context.Context, root *TwoFactorSettingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tfsgb.fns))
	for _, fn := range tfsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tfsgb.flds)+len(tfsgb.fns))
		for _, f := range *tfsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tfsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tfsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TwoFactorSettingSelect is the builder for selecting fields of TwoFactorSetting entities.
type TwoFactorSettingSelect struct {
	*TwoFactorSettingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tfss *TwoFactorSettingSelect) Aggregate(fns ...AggregateFunc) *TwoFactorSettingSelect {
	tfss.fns = append(tfss.fns, fns...)
	return tfss
}

// Scan applies the selector query and scans the result into the given value.
func (tfss *TwoFactorSettingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tfss.ctx, "Select")
	if err := tfss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TwoFactorSettingQuery, *TwoFactorSettingSelect](ctx, tfss.TwoFactorSettingQuery, tfss, tfss.inters, v)
}

func (tfss *TwoFactorSettingSelect) sqlScan(ctx context.Context, root *TwoFactorSettingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tfss.fns))
	for _, fn := range tfss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tfss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tfss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
