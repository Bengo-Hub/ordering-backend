// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/menuitem"
	"github.com/bengobox/ordering-backend/internal/ent/menuitemschedule"
	"github.com/google/uuid"
)

// MenuItemSchedule is the model entity for the MenuItemSchedule schema.
type MenuItemSchedule struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Reference to parent menu item
	MenuItemID uuid.UUID `json:"menu_item_id,omitempty"`
	// Day of week: 0=Sunday, 6=Saturday
	DayOfWeek int `json:"day_of_week,omitempty"`
	// Start time in HH:MM format
	TimeStart string `json:"time_start,omitempty"`
	// End time in HH:MM format
	TimeEnd string `json:"time_end,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MenuItemScheduleQuery when eager-loading is set.
	Edges        MenuItemScheduleEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MenuItemScheduleEdges holds the relations/edges for other nodes in the graph.
type MenuItemScheduleEdges struct {
	// MenuItem holds the value of the menu_item edge.
	MenuItem *MenuItem `json:"menu_item,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MenuItemOrErr returns the MenuItem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MenuItemScheduleEdges) MenuItemOrErr() (*MenuItem, error) {
	if e.MenuItem != nil {
		return e.MenuItem, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: menuitem.Label}
	}
	return nil, &NotLoadedError{edge: "menu_item"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MenuItemSchedule) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case menuitemschedule.FieldDayOfWeek:
			values[i] = new(sql.NullInt64)
		case menuitemschedule.FieldTimeStart, menuitemschedule.FieldTimeEnd:
			values[i] = new(sql.NullString)
		case menuitemschedule.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case menuitemschedule.FieldID, menuitemschedule.FieldMenuItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MenuItemSchedule fields.
func (mis *MenuItemSchedule) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case menuitemschedule.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				mis.ID = *value
			}
		case menuitemschedule.FieldMenuItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field menu_item_id", values[i])
			} else if value != nil {
				mis.MenuItemID = *value
			}
		case menuitemschedule.FieldDayOfWeek:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field day_of_week", values[i])
			} else if value.Valid {
				mis.DayOfWeek = int(value.Int64)
			}
		case menuitemschedule.FieldTimeStart:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field time_start", values[i])
			} else if value.Valid {
				mis.TimeStart = value.String
			}
		case menuitemschedule.FieldTimeEnd:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field time_end", values[i])
			} else if value.Valid {
				mis.TimeEnd = value.String
			}
		case menuitemschedule.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				mis.CreatedAt = value.Time
			}
		default:
			mis.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MenuItemSchedule.
// This includes values selected through modifiers, order, etc.
func (mis *MenuItemSchedule) Value(name string) (ent.Value, error) {
	return mis.selectValues.Get(name)
}

// QueryMenuItem queries the "menu_item" edge of the MenuItemSchedule entity.
func (mis *MenuItemSchedule) QueryMenuItem() *MenuItemQuery {
	return NewMenuItemScheduleClient(mis.config).QueryMenuItem(mis)
}

// Update returns a builder for updating this MenuItemSchedule.
// Note that you need to call MenuItemSchedule.Unwrap() before calling this method if this MenuItemSchedule
// was returned from a transaction, and the transaction was committed or rolled back.
func (mis *MenuItemSchedule) Update() *MenuItemScheduleUpdateOne {
	return NewMenuItemScheduleClient(mis.config).UpdateOne(mis)
}

// Unwrap unwraps the MenuItemSchedule entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mis *MenuItemSchedule) Unwrap() *MenuItemSchedule {
	_tx, ok := mis.config.driver.(*txDriver)
	if !ok {
		panic("ent: MenuItemSchedule is not a transactional entity")
	}
	mis.config.driver = _tx.drv
	return mis
}

// String implements the fmt.Stringer.
func (mis *MenuItemSchedule) String() string {
	var builder strings.Builder
	builder.WriteString("MenuItemSchedule(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mis.ID))
	builder.WriteString("menu_item_id=")
	builder.WriteString(fmt.Sprintf("%v", mis.MenuItemID))
	builder.WriteString(", ")
	builder.WriteString("day_of_week=")
	builder.WriteString(fmt.Sprintf("%v", mis.DayOfWeek))
	builder.WriteString(", ")
	builder.WriteString("time_start=")
	builder.WriteString(mis.TimeStart)
	builder.WriteString(", ")
	builder.WriteString("time_end=")
	builder.WriteString(mis.TimeEnd)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(mis.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MenuItemSchedules is a parsable slice of MenuItemSchedule.
type MenuItemSchedules []*MenuItemSchedule
