// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/bengobox/ordering-backend/internal/ent/orderassignment"
	"github.com/bengobox/ordering-backend/internal/ent/predicate"
	"github.com/bengobox/ordering-backend/internal/ent/proofofdelivery"
	"github.com/google/uuid"
)

// ProofOfDeliveryQuery is the builder for querying ProofOfDelivery entities.
type ProofOfDeliveryQuery struct {
	config
	ctx            *QueryContext
	order          []proofofdelivery.OrderOption
	inters         []Interceptor
	predicates     []predicate.ProofOfDelivery
	withAssignment *OrderAssignmentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProofOfDeliveryQuery builder.
func (podq *ProofOfDeliveryQuery) Where(ps ...predicate.ProofOfDelivery) *ProofOfDeliveryQuery {
	podq.predicates = append(podq.predicates, ps...)
	return podq
}

// Limit the number of records to be returned by this query.
func (podq *ProofOfDeliveryQuery) Limit(limit int) *ProofOfDeliveryQuery {
	podq.ctx.Limit = &limit
	return podq
}

// Offset to start from.
func (podq *ProofOfDeliveryQuery) Offset(offset int) *ProofOfDeliveryQuery {
	podq.ctx.Offset = &offset
	return podq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (podq *ProofOfDeliveryQuery) Unique(unique bool) *ProofOfDeliveryQuery {
	podq.ctx.Unique = &unique
	return podq
}

// Order specifies how the records should be ordered.
func (podq *ProofOfDeliveryQuery) Order(o ...proofofdelivery.OrderOption) *ProofOfDeliveryQuery {
	podq.order = append(podq.order, o...)
	return podq
}

// QueryAssignment chains the current query on the "assignment" edge.
func (podq *ProofOfDeliveryQuery) QueryAssignment() *OrderAssignmentQuery {
	query := (&OrderAssignmentClient{config: podq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := podq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := podq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(proofofdelivery.Table, proofofdelivery.FieldID, selector),
			sqlgraph.To(orderassignment.Table, orderassignment.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, proofofdelivery.AssignmentTable, proofofdelivery.AssignmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(podq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProofOfDelivery entity from the query.
// Returns a *NotFoundError when no ProofOfDelivery was found.
func (podq *ProofOfDeliveryQuery) First(ctx context.Context) (*ProofOfDelivery, error) {
	nodes, err := podq.Limit(1).All(setContextOp(ctx, podq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{proofofdelivery.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) FirstX(ctx context.Context) *ProofOfDelivery {
	node, err := podq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProofOfDelivery ID from the query.
// Returns a *NotFoundError when no ProofOfDelivery ID was found.
func (podq *ProofOfDeliveryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = podq.Limit(1).IDs(setContextOp(ctx, podq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{proofofdelivery.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := podq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProofOfDelivery entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProofOfDelivery entity is found.
// Returns a *NotFoundError when no ProofOfDelivery entities are found.
func (podq *ProofOfDeliveryQuery) Only(ctx context.Context) (*ProofOfDelivery, error) {
	nodes, err := podq.Limit(2).All(setContextOp(ctx, podq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{proofofdelivery.Label}
	default:
		return nil, &NotSingularError{proofofdelivery.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) OnlyX(ctx context.Context) *ProofOfDelivery {
	node, err := podq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProofOfDelivery ID in the query.
// Returns a *NotSingularError when more than one ProofOfDelivery ID is found.
// Returns a *NotFoundError when no entities are found.
func (podq *ProofOfDeliveryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = podq.Limit(2).IDs(setContextOp(ctx, podq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{proofofdelivery.Label}
	default:
		err = &NotSingularError{proofofdelivery.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := podq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProofOfDeliveries.
func (podq *ProofOfDeliveryQuery) All(ctx context.Context) ([]*ProofOfDelivery, error) {
	ctx = setContextOp(ctx, podq.ctx, "All")
	if err := podq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProofOfDelivery, *ProofOfDeliveryQuery]()
	return withInterceptors[[]*ProofOfDelivery](ctx, podq, qr, podq.inters)
}

// AllX is like All, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) AllX(ctx context.Context) []*ProofOfDelivery {
	nodes, err := podq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProofOfDelivery IDs.
func (podq *ProofOfDeliveryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if podq.ctx.Unique == nil && podq.path != nil {
		podq.Unique(true)
	}
	ctx = setContextOp(ctx, podq.ctx, "IDs")
	if err = podq.Select(proofofdelivery.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := podq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (podq *ProofOfDeliveryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, podq.ctx, "Count")
	if err := podq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, podq, querierCount[*ProofOfDeliveryQuery](), podq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) CountX(ctx context.Context) int {
	count, err := podq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (podq *ProofOfDeliveryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, podq.ctx, "Exist")
	switch _, err := podq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (podq *ProofOfDeliveryQuery) ExistX(ctx context.Context) bool {
	exist, err := podq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProofOfDeliveryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (podq *ProofOfDeliveryQuery) Clone() *ProofOfDeliveryQuery {
	if podq == nil {
		return nil
	}
	return &ProofOfDeliveryQuery{
		config:         podq.config,
		ctx:            podq.ctx.Clone(),
		order:          append([]proofofdelivery.OrderOption{}, podq.order...),
		inters:         append([]Interceptor{}, podq.inters...),
		predicates:     append([]predicate.ProofOfDelivery{}, podq.predicates...),
		withAssignment: podq.withAssignment.Clone(),
		// clone intermediate query.
		sql:  podq.sql.Clone(),
		path: podq.path,
	}
}

// WithAssignment tells the query-builder to eager-load the nodes that are connected to
// the "assignment" edge. The optional arguments are used to configure the query builder of the edge.
func (podq *ProofOfDeliveryQuery) WithAssignment(opts ...func(*OrderAssignmentQuery)) *ProofOfDeliveryQuery {
	query := (&OrderAssignmentClient{config: podq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	podq.withAssignment = query
	return podq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProofOfDelivery.Query().
//		GroupBy(proofofdelivery.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (podq *ProofOfDeliveryQuery) GroupBy(field string, fields ...string) *ProofOfDeliveryGroupBy {
	podq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProofOfDeliveryGroupBy{build: podq}
	grbuild.flds = &podq.ctx.Fields
	grbuild.label = proofofdelivery.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID uuid.UUID `json:"tenant_id,omitempty"`
//	}
//
//	client.ProofOfDelivery.Query().
//		Select(proofofdelivery.FieldTenantID).
//		Scan(ctx, &v)
func (podq *ProofOfDeliveryQuery) Select(fields ...string) *ProofOfDeliverySelect {
	podq.ctx.Fields = append(podq.ctx.Fields, fields...)
	sbuild := &ProofOfDeliverySelect{ProofOfDeliveryQuery: podq}
	sbuild.label = proofofdelivery.Label
	sbuild.flds, sbuild.scan = &podq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProofOfDeliverySelect configured with the given aggregations.
func (podq *ProofOfDeliveryQuery) Aggregate(fns ...AggregateFunc) *ProofOfDeliverySelect {
	return podq.Select().Aggregate(fns...)
}

func (podq *ProofOfDeliveryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range podq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, podq); err != nil {
				return err
			}
		}
	}
	for _, f := range podq.ctx.Fields {
		if !proofofdelivery.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if podq.path != nil {
		prev, err := podq.path(ctx)
		if err != nil {
			return err
		}
		podq.sql = prev
	}
	return nil
}

func (podq *ProofOfDeliveryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProofOfDelivery, error) {
	var (
		nodes       = []*ProofOfDelivery{}
		_spec       = podq.querySpec()
		loadedTypes = [1]bool{
			podq.withAssignment != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProofOfDelivery).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProofOfDelivery{config: podq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, podq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := podq.withAssignment; query != nil {
		if err := podq.loadAssignment(ctx, query, nodes, nil,
			func(n *ProofOfDelivery, e *OrderAssignment) { n.Edges.Assignment = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (podq *ProofOfDeliveryQuery) loadAssignment(ctx context.Context, query *OrderAssignmentQuery, nodes []*ProofOfDelivery, init func(*ProofOfDelivery), assign func(*ProofOfDelivery, *OrderAssignment)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProofOfDelivery)
	for i := range nodes {
		fk := nodes[i].AssignmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(orderassignment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "assignment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (podq *ProofOfDeliveryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := podq.querySpec()
	_spec.Node.Columns = podq.ctx.Fields
	if len(podq.ctx.Fields) > 0 {
		_spec.Unique = podq.ctx.Unique != nil && *podq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, podq.driver, _spec)
}

func (podq *ProofOfDeliveryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(proofofdelivery.Table, proofofdelivery.Columns, sqlgraph.NewFieldSpec(proofofdelivery.FieldID, field.TypeUUID))
	_spec.From = podq.sql
	if unique := podq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if podq.path != nil {
		_spec.Unique = true
	}
	if fields := podq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, proofofdelivery.FieldID)
		for i := range fields {
			if fields[i] != proofofdelivery.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if podq.withAssignment != nil {
			_spec.Node.AddColumnOnce(proofofdelivery.FieldAssignmentID)
		}
	}
	if ps := podq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := podq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := podq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := podq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (podq *ProofOfDeliveryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(podq.driver.Dialect())
	t1 := builder.Table(proofofdelivery.Table)
	columns := podq.ctx.Fields
	if len(columns) == 0 {
		columns = proofofdelivery.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if podq.sql != nil {
		selector = podq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if podq.ctx.Unique != nil && *podq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range podq.predicates {
		p(selector)
	}
	for _, p := range podq.order {
		p(selector)
	}
	if offset := podq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := podq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProofOfDeliveryGroupBy is the group-by builder for ProofOfDelivery entities.
type ProofOfDeliveryGroupBy struct {
	selector
	build *ProofOfDeliveryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (podgb *ProofOfDeliveryGroupBy) Aggregate(fns ...AggregateFunc) *ProofOfDeliveryGroupBy {
	podgb.fns = append(podgb.fns, fns...)
	return podgb
}

// Scan applies the selector query and scans the result into the given value.
func (podgb *ProofOfDeliveryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, podgb.build.ctx, "GroupBy")
	if err := podgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProofOfDeliveryQuery, *ProofOfDeliveryGroupBy](ctx, podgb.build, podgb, podgb.build.inters, v)
}

func (podgb *ProofOfDeliveryGroupBy) sqlScan(ctx context.Context, root *ProofOfDeliveryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(podgb.fns))
	for _, fn := range podgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*podgb.flds)+len(podgb.fns))
		for _, f := range *podgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*podgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := podgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProofOfDeliverySelect is the builder for selecting fields of ProofOfDelivery entities.
type ProofOfDeliverySelect struct {
	*ProofOfDeliveryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pods *ProofOfDeliverySelect) Aggregate(fns ...AggregateFunc) *ProofOfDeliverySelect {
	pods.fns = append(pods.fns, fns...)
	return pods
}

// Scan applies the selector query and scans the result into the given value.
func (pods *ProofOfDeliverySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pods.ctx, "Select")
	if err := pods.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProofOfDeliveryQuery, *ProofOfDeliverySelect](ctx, pods.ProofOfDeliveryQuery, pods, pods.inters, v)
}

func (pods *ProofOfDeliverySelect) sqlScan(ctx context.Context, root *ProofOfDeliveryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pods.fns))
	for _, fn := range pods.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pods.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pods.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
