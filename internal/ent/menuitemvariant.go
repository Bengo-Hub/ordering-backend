// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/ordering-backend/internal/ent/menuitem"
	"github.com/bengobox/ordering-backend/internal/ent/menuitemvariant"
	"github.com/google/uuid"
)

// MenuItemVariant is the model entity for the MenuItemVariant schema.
type MenuItemVariant struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Reference to parent menu item
	MenuItemID uuid.UUID `json:"menu_item_id,omitempty"`
	// Variant name, e.g., 'Large', 'Extra Cheese'
	Name string `json:"name,omitempty"`
	// Price difference from base price (can be negative)
	PriceDelta float64 `json:"price_delta,omitempty"`
	// IsAvailable holds the value of the "is_available" field.
	IsAvailable bool `json:"is_available,omitempty"`
	// SKU for inventory tracking
	Sku string `json:"sku,omitempty"`
	// DisplayOrder holds the value of the "display_order" field.
	DisplayOrder int `json:"display_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MenuItemVariantQuery when eager-loading is set.
	Edges        MenuItemVariantEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MenuItemVariantEdges holds the relations/edges for other nodes in the graph.
type MenuItemVariantEdges struct {
	// MenuItem holds the value of the menu_item edge.
	MenuItem *MenuItem `json:"menu_item,omitempty"`
	// CartItems holds the value of the cart_items edge.
	CartItems []*CartItem `json:"cart_items,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// MenuItemOrErr returns the MenuItem value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MenuItemVariantEdges) MenuItemOrErr() (*MenuItem, error) {
	if e.MenuItem != nil {
		return e.MenuItem, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: menuitem.Label}
	}
	return nil, &NotLoadedError{edge: "menu_item"}
}

// CartItemsOrErr returns the CartItems value or an error if the edge
// was not loaded in eager-loading.
func (e MenuItemVariantEdges) CartItemsOrErr() ([]*CartItem, error) {
	if e.loadedTypes[1] {
		return e.CartItems, nil
	}
	return nil, &NotLoadedError{edge: "cart_items"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MenuItemVariant) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case menuitemvariant.FieldIsAvailable:
			values[i] = new(sql.NullBool)
		case menuitemvariant.FieldPriceDelta:
			values[i] = new(sql.NullFloat64)
		case menuitemvariant.FieldDisplayOrder:
			values[i] = new(sql.NullInt64)
		case menuitemvariant.FieldName, menuitemvariant.FieldSku:
			values[i] = new(sql.NullString)
		case menuitemvariant.FieldCreatedAt, menuitemvariant.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case menuitemvariant.FieldID, menuitemvariant.FieldMenuItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MenuItemVariant fields.
func (miv *MenuItemVariant) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case menuitemvariant.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				miv.ID = *value
			}
		case menuitemvariant.FieldMenuItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field menu_item_id", values[i])
			} else if value != nil {
				miv.MenuItemID = *value
			}
		case menuitemvariant.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				miv.Name = value.String
			}
		case menuitemvariant.FieldPriceDelta:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price_delta", values[i])
			} else if value.Valid {
				miv.PriceDelta = value.Float64
			}
		case menuitemvariant.FieldIsAvailable:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_available", values[i])
			} else if value.Valid {
				miv.IsAvailable = value.Bool
			}
		case menuitemvariant.FieldSku:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sku", values[i])
			} else if value.Valid {
				miv.Sku = value.String
			}
		case menuitemvariant.FieldDisplayOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field display_order", values[i])
			} else if value.Valid {
				miv.DisplayOrder = int(value.Int64)
			}
		case menuitemvariant.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				miv.CreatedAt = value.Time
			}
		case menuitemvariant.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				miv.UpdatedAt = value.Time
			}
		default:
			miv.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MenuItemVariant.
// This includes values selected through modifiers, order, etc.
func (miv *MenuItemVariant) Value(name string) (ent.Value, error) {
	return miv.selectValues.Get(name)
}

// QueryMenuItem queries the "menu_item" edge of the MenuItemVariant entity.
func (miv *MenuItemVariant) QueryMenuItem() *MenuItemQuery {
	return NewMenuItemVariantClient(miv.config).QueryMenuItem(miv)
}

// QueryCartItems queries the "cart_items" edge of the MenuItemVariant entity.
func (miv *MenuItemVariant) QueryCartItems() *CartItemQuery {
	return NewMenuItemVariantClient(miv.config).QueryCartItems(miv)
}

// Update returns a builder for updating this MenuItemVariant.
// Note that you need to call MenuItemVariant.Unwrap() before calling this method if this MenuItemVariant
// was returned from a transaction, and the transaction was committed or rolled back.
func (miv *MenuItemVariant) Update() *MenuItemVariantUpdateOne {
	return NewMenuItemVariantClient(miv.config).UpdateOne(miv)
}

// Unwrap unwraps the MenuItemVariant entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (miv *MenuItemVariant) Unwrap() *MenuItemVariant {
	_tx, ok := miv.config.driver.(*txDriver)
	if !ok {
		panic("ent: MenuItemVariant is not a transactional entity")
	}
	miv.config.driver = _tx.drv
	return miv
}

// String implements the fmt.Stringer.
func (miv *MenuItemVariant) String() string {
	var builder strings.Builder
	builder.WriteString("MenuItemVariant(")
	builder.WriteString(fmt.Sprintf("id=%v, ", miv.ID))
	builder.WriteString("menu_item_id=")
	builder.WriteString(fmt.Sprintf("%v", miv.MenuItemID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(miv.Name)
	builder.WriteString(", ")
	builder.WriteString("price_delta=")
	builder.WriteString(fmt.Sprintf("%v", miv.PriceDelta))
	builder.WriteString(", ")
	builder.WriteString("is_available=")
	builder.WriteString(fmt.Sprintf("%v", miv.IsAvailable))
	builder.WriteString(", ")
	builder.WriteString("sku=")
	builder.WriteString(miv.Sku)
	builder.WriteString(", ")
	builder.WriteString("display_order=")
	builder.WriteString(fmt.Sprintf("%v", miv.DisplayOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(miv.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(miv.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MenuItemVariants is a parsable slice of MenuItemVariant.
type MenuItemVariants []*MenuItemVariant
