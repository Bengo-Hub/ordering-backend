// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/cafe-backend/internal/ent/riderdocument"
	"github.com/google/uuid"
)

// RiderDocument is the model entity for the RiderDocument schema.
type RiderDocument struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// DocumentType holds the value of the "document_type" field.
	DocumentType string `json:"document_type,omitempty"`
	// FileURL holds the value of the "file_url" field.
	FileURL string `json:"file_url,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// VerifiedAt holds the value of the "verified_at" field.
	VerifiedAt time.Time `json:"verified_at,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RiderDocumentQuery when eager-loading is set.
	Edges        RiderDocumentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RiderDocumentEdges holds the relations/edges for other nodes in the graph.
type RiderDocumentEdges struct {
	// Rider holds the value of the rider edge.
	Rider []*RiderProfile `json:"rider,omitempty"`
	// Reviewer holds the value of the reviewer edge.
	Reviewer []*User `json:"reviewer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RiderOrErr returns the Rider value or an error if the edge
// was not loaded in eager-loading.
func (e RiderDocumentEdges) RiderOrErr() ([]*RiderProfile, error) {
	if e.loadedTypes[0] {
		return e.Rider, nil
	}
	return nil, &NotLoadedError{edge: "rider"}
}

// ReviewerOrErr returns the Reviewer value or an error if the edge
// was not loaded in eager-loading.
func (e RiderDocumentEdges) ReviewerOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.Reviewer, nil
	}
	return nil, &NotLoadedError{edge: "reviewer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RiderDocument) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case riderdocument.FieldMetadata:
			values[i] = new([]byte)
		case riderdocument.FieldDocumentType, riderdocument.FieldFileURL, riderdocument.FieldStatus:
			values[i] = new(sql.NullString)
		case riderdocument.FieldExpiresAt, riderdocument.FieldVerifiedAt, riderdocument.FieldCreatedAt, riderdocument.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case riderdocument.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RiderDocument fields.
func (rd *RiderDocument) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case riderdocument.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				rd.ID = *value
			}
		case riderdocument.FieldDocumentType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field document_type", values[i])
			} else if value.Valid {
				rd.DocumentType = value.String
			}
		case riderdocument.FieldFileURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_url", values[i])
			} else if value.Valid {
				rd.FileURL = value.String
			}
		case riderdocument.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				rd.Status = value.String
			}
		case riderdocument.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				rd.ExpiresAt = value.Time
			}
		case riderdocument.FieldVerifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field verified_at", values[i])
			} else if value.Valid {
				rd.VerifiedAt = value.Time
			}
		case riderdocument.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &rd.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case riderdocument.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rd.CreatedAt = value.Time
			}
		case riderdocument.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rd.UpdatedAt = value.Time
			}
		default:
			rd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RiderDocument.
// This includes values selected through modifiers, order, etc.
func (rd *RiderDocument) Value(name string) (ent.Value, error) {
	return rd.selectValues.Get(name)
}

// QueryRider queries the "rider" edge of the RiderDocument entity.
func (rd *RiderDocument) QueryRider() *RiderProfileQuery {
	return NewRiderDocumentClient(rd.config).QueryRider(rd)
}

// QueryReviewer queries the "reviewer" edge of the RiderDocument entity.
func (rd *RiderDocument) QueryReviewer() *UserQuery {
	return NewRiderDocumentClient(rd.config).QueryReviewer(rd)
}

// Update returns a builder for updating this RiderDocument.
// Note that you need to call RiderDocument.Unwrap() before calling this method if this RiderDocument
// was returned from a transaction, and the transaction was committed or rolled back.
func (rd *RiderDocument) Update() *RiderDocumentUpdateOne {
	return NewRiderDocumentClient(rd.config).UpdateOne(rd)
}

// Unwrap unwraps the RiderDocument entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rd *RiderDocument) Unwrap() *RiderDocument {
	_tx, ok := rd.config.driver.(*txDriver)
	if !ok {
		panic("ent: RiderDocument is not a transactional entity")
	}
	rd.config.driver = _tx.drv
	return rd
}

// String implements the fmt.Stringer.
func (rd *RiderDocument) String() string {
	var builder strings.Builder
	builder.WriteString("RiderDocument(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rd.ID))
	builder.WriteString("document_type=")
	builder.WriteString(rd.DocumentType)
	builder.WriteString(", ")
	builder.WriteString("file_url=")
	builder.WriteString(rd.FileURL)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(rd.Status)
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(rd.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("verified_at=")
	builder.WriteString(rd.VerifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", rd.Metadata))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rd.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rd.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RiderDocuments is a parsable slice of RiderDocument.
type RiderDocuments []*RiderDocument
