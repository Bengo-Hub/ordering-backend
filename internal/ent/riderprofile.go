// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/cafe-backend/internal/ent/riderprofile"
	"github.com/bengobox/cafe-backend/internal/ent/user"
	"github.com/google/uuid"
)

// RiderProfile is the model entity for the RiderProfile schema.
type RiderProfile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// NationalID holds the value of the "national_id" field.
	NationalID string `json:"national_id,omitempty"`
	// LicenseNumber holds the value of the "license_number" field.
	LicenseNumber string `json:"license_number,omitempty"`
	// VehicleType holds the value of the "vehicle_type" field.
	VehicleType string `json:"vehicle_type,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Rating holds the value of the "rating" field.
	Rating float64 `json:"rating,omitempty"`
	// OnboardedAt holds the value of the "onboarded_at" field.
	OnboardedAt time.Time `json:"onboarded_at,omitempty"`
	// SuspendedAt holds the value of the "suspended_at" field.
	SuspendedAt time.Time `json:"suspended_at,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RiderProfileQuery when eager-loading is set.
	Edges              RiderProfileEdges `json:"edges"`
	user_rider_profile *uuid.UUID
	selectValues       sql.SelectValues
}

// RiderProfileEdges holds the relations/edges for other nodes in the graph.
type RiderProfileEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Tenant holds the value of the tenant edge.
	Tenant []*Tenant `json:"tenant,omitempty"`
	// Documents holds the value of the documents edge.
	Documents []*RiderDocument `json:"documents,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RiderProfileEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading.
func (e RiderProfileEdges) TenantOrErr() ([]*Tenant, error) {
	if e.loadedTypes[1] {
		return e.Tenant, nil
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// DocumentsOrErr returns the Documents value or an error if the edge
// was not loaded in eager-loading.
func (e RiderProfileEdges) DocumentsOrErr() ([]*RiderDocument, error) {
	if e.loadedTypes[2] {
		return e.Documents, nil
	}
	return nil, &NotLoadedError{edge: "documents"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RiderProfile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case riderprofile.FieldMetadata:
			values[i] = new([]byte)
		case riderprofile.FieldRating:
			values[i] = new(sql.NullFloat64)
		case riderprofile.FieldID:
			values[i] = new(sql.NullInt64)
		case riderprofile.FieldNationalID, riderprofile.FieldLicenseNumber, riderprofile.FieldVehicleType, riderprofile.FieldStatus:
			values[i] = new(sql.NullString)
		case riderprofile.FieldOnboardedAt, riderprofile.FieldSuspendedAt, riderprofile.FieldCreatedAt, riderprofile.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case riderprofile.ForeignKeys[0]: // user_rider_profile
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RiderProfile fields.
func (rp *RiderProfile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case riderprofile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rp.ID = int(value.Int64)
		case riderprofile.FieldNationalID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field national_id", values[i])
			} else if value.Valid {
				rp.NationalID = value.String
			}
		case riderprofile.FieldLicenseNumber:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field license_number", values[i])
			} else if value.Valid {
				rp.LicenseNumber = value.String
			}
		case riderprofile.FieldVehicleType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vehicle_type", values[i])
			} else if value.Valid {
				rp.VehicleType = value.String
			}
		case riderprofile.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				rp.Status = value.String
			}
		case riderprofile.FieldRating:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field rating", values[i])
			} else if value.Valid {
				rp.Rating = value.Float64
			}
		case riderprofile.FieldOnboardedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field onboarded_at", values[i])
			} else if value.Valid {
				rp.OnboardedAt = value.Time
			}
		case riderprofile.FieldSuspendedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field suspended_at", values[i])
			} else if value.Valid {
				rp.SuspendedAt = value.Time
			}
		case riderprofile.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &rp.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case riderprofile.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rp.CreatedAt = value.Time
			}
		case riderprofile.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rp.UpdatedAt = value.Time
			}
		case riderprofile.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_rider_profile", values[i])
			} else if value.Valid {
				rp.user_rider_profile = new(uuid.UUID)
				*rp.user_rider_profile = *value.S.(*uuid.UUID)
			}
		default:
			rp.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RiderProfile.
// This includes values selected through modifiers, order, etc.
func (rp *RiderProfile) Value(name string) (ent.Value, error) {
	return rp.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the RiderProfile entity.
func (rp *RiderProfile) QueryUser() *UserQuery {
	return NewRiderProfileClient(rp.config).QueryUser(rp)
}

// QueryTenant queries the "tenant" edge of the RiderProfile entity.
func (rp *RiderProfile) QueryTenant() *TenantQuery {
	return NewRiderProfileClient(rp.config).QueryTenant(rp)
}

// QueryDocuments queries the "documents" edge of the RiderProfile entity.
func (rp *RiderProfile) QueryDocuments() *RiderDocumentQuery {
	return NewRiderProfileClient(rp.config).QueryDocuments(rp)
}

// Update returns a builder for updating this RiderProfile.
// Note that you need to call RiderProfile.Unwrap() before calling this method if this RiderProfile
// was returned from a transaction, and the transaction was committed or rolled back.
func (rp *RiderProfile) Update() *RiderProfileUpdateOne {
	return NewRiderProfileClient(rp.config).UpdateOne(rp)
}

// Unwrap unwraps the RiderProfile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rp *RiderProfile) Unwrap() *RiderProfile {
	_tx, ok := rp.config.driver.(*txDriver)
	if !ok {
		panic("ent: RiderProfile is not a transactional entity")
	}
	rp.config.driver = _tx.drv
	return rp
}

// String implements the fmt.Stringer.
func (rp *RiderProfile) String() string {
	var builder strings.Builder
	builder.WriteString("RiderProfile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rp.ID))
	builder.WriteString("national_id=")
	builder.WriteString(rp.NationalID)
	builder.WriteString(", ")
	builder.WriteString("license_number=")
	builder.WriteString(rp.LicenseNumber)
	builder.WriteString(", ")
	builder.WriteString("vehicle_type=")
	builder.WriteString(rp.VehicleType)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(rp.Status)
	builder.WriteString(", ")
	builder.WriteString("rating=")
	builder.WriteString(fmt.Sprintf("%v", rp.Rating))
	builder.WriteString(", ")
	builder.WriteString("onboarded_at=")
	builder.WriteString(rp.OnboardedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("suspended_at=")
	builder.WriteString(rp.SuspendedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", rp.Metadata))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rp.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rp.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RiderProfiles is a parsable slice of RiderProfile.
type RiderProfiles []*RiderProfile
